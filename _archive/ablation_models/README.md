# ablation_models/ — A~D축 시나리오별 모델

`docs/04` A축 표(A0~A8)와 `docs/05` D축 표(D0~D8)의 gain·AUC 가 이 모델들에서 나왔다.
`step30_stage6_ablation.py` / `step34_d_axis.py` 재실행으로 재생성 가능하나 시드 차이로
소수점이 미세하게 달라질 수 있어 보관한다. **인용 시 조건(시드·규제) 병기 필수.**
