# legacy_panels/ — 구세대 패널

`nh_panel_full.csv` (STAGE 6 S0 베이스라인). `eda_pipeline/config.py` 의
`panel_path("legacy")` 가 이 경로를 가리킨다. 재현 시 `eda_pipeline` step1~2 로
재생성 가능. `.gitignore(*.csv)` 대상이라 git 추적되지 않는다.
