# 매핑 오류 영향 범위 — STAGE 6 이후 재검증 대상

JEMU 계정 코드 매핑이 `118100` 부터 한 칸씩 밀려 있었다.
`input/가상사업자_JEMU_재무데이터v.txt` 0행(한글 논리명)이 정본이며,
28개 중 21개 라벨이 어긋나 있었다 (예: `191204` 를 "ROE" 로, `191208` 을 "매출채권회전율" 로).

표시 라벨뿐 아니라 `backend/routers/borrowers.py` 의 **실제 데이터 매핑**도 같은 오류를
갖고 있어, 포털 차주 상세 화면이 잘못된 값을 표시하고 있었다.

| # | 대상 | 상태 | 비고 |
|---|---|---|---|
| 1 | 포털 차주 상세 재무 요약 4개 값 | 확인 전 | 수정 완료, 재확인 필요 (capital/revenue/operating_profit) |
| 2 | 포털 레이더 차트 11개 축 | 확인 전 | 수정 완료 (10축 정정 + 1축 제거 -> 10축) |
| 3 | README.md 포털 관련 지표 | 확인 전 | |
| 4 | MODEL_EVALUATION_REPORT.md | 확인 전 | |
| 5 | docs/ 산출물 (step01~step29 중 JEMU 라벨 인용 문서) | 확인 전 | |
| 6 | eda_report.html 및 플롯 8종 | 확인 전 | |

문서 파일 자체는 아직 수정하지 않았다. STAGE 6 이후 위 항목을 하나씩 확인하고
상태를 "영향 있음" / "영향 없음" 으로 갱신한다.

## STAGE 6 선행 필수

- `portal.duckdb` 를 새 패널 스키마로 재빌드한다 (`JEMU_191506_val` 등 신규 컬럼).
- `database/db_migration.py` / `database/init_duckdb.py` 를 갱신한다.
- 재빌드 전까지 backend 는 구 스키마 기준으로만 동작하며, 레이더 SQL 은
  `Binder Error: column JEMU_191506_val not found` 가 예상된다. **정상이다.**
- 재빌드 시 기존 `portal.duckdb` 를 덮어쓰지 말고 **`portal_v2.duckdb`** 로 생성한다.

현재는 코드만 새 컬럼명 기준으로 맞춰 두었고, 실행 검증은 STAGE 6 에서 한다.

## 레이더 축 최종 구성 (STAGE 5 시점)

| 그룹 | 축 | 참조 |
|---|---|---|
| 활동성 | pr_asset_turnover / pr_receivable_turnover / pr_inventory_turnover | 191506_val / 191502_val / 191505_val |
| 수익성 | pr_op_margin / pr_roe | 191204_val / 191208_val |
| 안정성 | pr_debt_ratio_inv / pr_current_ratio / pr_interest_coverage | -JEMU_debt_ratio / JEMU_current_ratio / 191207_val |
| 성장성 | pr_revenue_growth | 191104_val |
| 규모 | pr_assets | JEMU_115000 |

축 개수·구성 최적화는 STAGE 6 에서 SHAP 상위 변수가 확정된 뒤 재설계한다.

## 거시 데이터 재현성 결함 (STAGE 5 발견)

`api_data_processing/output/model_input/model_input_monthly_cleaned.csv` 가 존재하지 않는다.
`api_data_processing/` 에는 `assign_virtual_branch.py` 하나뿐이고 **거시 데이터 수집
스크립트가 레포에 없다.**

- `.gitignore:29` 에 `*.csv` 가 있어 커밋 대상에서 제외되어 있다.
  (커밋된 적이 없는 것이 아니라, 로컬 산출물로만 존재했고 gitignore 로 빠진 것이다.
   `portal.duckdb`, `nh_panel_full.csv` 와 같은 경우다.)
- 상위 폴더(`sy/NH AI경진대회/`) 전체를 탐색했으나 거시 CSV 는 발견되지 않았다.
  거시 변수명이 남아 있는 곳은 `lgbm_12m_model.txt` / `lgbm_12m_lean_model.txt` 뿐이다.

**거시 데이터의 출처·수집일·산출 로직이 현재 재현 불가능하다.**
프로젝트 재현성 측면에서 중대한 결함이며, 아래가 필요하다.

| # | 필요 조치 | 상태 |
|---|---|---|
| 7 | 거시 데이터 수집 스크립트 복원 또는 재작성 | **진행 중** — `api_data_processing/collect_macro.py` 신규 작성 (2026-08-30). 수집/변환 2단계 분리, 원시는 `raw/` 에 수집일 박아 저장. yfinance 21개 READY / ECOS 40개는 통계표코드 확인 필요 / 파생 2개 미해결 |
| 8 | `model_input_monthly_cleaned.csv` 원본 확보 | **미확보** — 재수집으로 대체한다. 7번 완료 시 `--transform` 으로 생성 |
| 9 | 거시 172개 변수의 정의·출처·산출식 문서화 | **완료(역파싱분)** — `eda_pipeline/output/macro_respec_from_model.json`. 172개 = 원지표 63개 x 변환 2~4종. 변환 정의는 `collect_macro.transform()` docstring |

### 7번 잔여 작업

| 항목 | 개수 | 막힌 이유 |
|---|---:|---|
| yfinance | 21 | 없음. 바로 수집 가능 |
| ECOS | 40 | 통계표코드/항목코드 미확인. 통계목록 API 로 조회해 채울 것. 추측값을 코드에 넣지 않았다 |
| 파생 (`credit_spread`, `liquidity_spread`) | 2 | 어느 두 금리의 차인지 산출식이 문서에 없다 |

추가 확인 필요 2건: `treasury_bond_1y` vs `treasury_bond_1y_monthly`,
`current_account` vs `current_account_quarterly` 의 차이.

STAGE 5 의 5-2 / 5-4 / 5-5 는 합성 거시 프레임으로 로직만 검증했다.
합성 프레임 생성 규칙: `lgbm_12m_model.txt` 에서 거시 변수 172개 이름을 추출,
BASE_YM 202101~202505(53개월), `numpy.random.default_rng(20260829)` 고정 시드.
파일명은 `macro_SYNTHETIC_DO_NOT_USE_FOR_TRAINING.csv` 이며 **학습에 사용 금지**다.

### 5-3 업종별 거시 민감도 — STAGE 6 보류

`STD_INDS_CFC` 대분류 x 주요 거시지표의 Train 구간 민감도 계수 산출은
거시 실데이터가 없으면 불가능하다. 거시 파일 확보 후 STAGE 6 에서 착수한다.

| # | 필요 조치 | 상태 |
|---|---|---|
| 10 | 5-3 업종별 거시 민감도 계수 산출 (거시 실데이터 필요) | 보류 |

## STAGE 6 선행 작업 목록 (확정)

| # | 작업 | 비고 |
|---|---|---|
| 1 | `portal.duckdb` 를 새 패널 스키마로 재빌드 | **`portal_v2.duckdb`** 로 생성. 기존 파일 덮어쓰지 말 것 |
| 2 | `db_migration.py` / `init_duckdb.py` 갱신 | Parquet 직접 읽기 가능 |
| 3 | `step7_modeling_shap.py` 경로를 `config.read_panel()` 로 전환 | 현재 구 파일명 `nh_panel_macro_12m.csv` 를 봄 |
| 4 | `validation_common.py` 하드코딩 절대경로 4건 정리 | `C:/Users/User/model_kbm/...` — 다른 계정 경로라 이 PC에서 동작 불가 |
| 5 | `step7` 피처 선정 로직 수정 | 현재 `ignore_cols` 4개뿐. `LEAK_CONFIRMED` + `NON_FEATURE` 제외 적용 필요 |
| 6 | `step7` early stopping 이 Valid 를 보지 않도록 Dev셋 사용 확인 | |

`step8`~`step15` 의 `.gemini/antigravity/brain` 경로 2건은 이번 리팩터링 범위 밖이다.
**건드리지 않고 목록에만 기록한다.**

## STAGE 5 에서 발견한 -1 잔재 (조치 대기)

`STD_INDS_CFC` 의 유효하지 않은 813행은 `-1` 765건 / `0` 34건 / `25` 14건이었다.
`-1` 은 step5 의 범주형 결측 대체값이다.

전수 점검 결과, **`CG01_KIS_SCORE` 140,419행(14.81%)이 `-1`** 이다.
`step1_load.py:345` 의 `fillna(-1).astype(int)` 때문이며, 이는 '평가 이력 없음'을
실제 점수 -1 로 만든 것이다. STAGE 3 에서 이 변수를 '유형 3(진짜 결측) → NaN 유지' 로
처리했으나, step1 에서 이미 -1 로 채워져 NaN 이 존재하지 않아 그 처리가 무효였다.
`CG01_KIS_SCORE` 는 기존 모델 gain 2위(14.5%)이므로 영향이 크다. **조치 승인 대기.**

`JEMU_pl_turn_dir`(-1 = 흑자→적자), `_val` 계열, `AA17_YOY_*`(-100%) 의 -1 은
실제 값이므로 조치 대상이 아니다.

## STAGE 6 시나리오 — 중요도 아티팩트 분리 검증 (제안 58)

`CG01_KIS_SCORE` 의 기존 모델 gain 14.51% 에는 성격이 다른 두 원인이 섞여 있을 수 있다.

- **(a) 시점 누수** — 연 단위 조인으로 최종재무평점_2021 이 2021년 1월에 붙는다.
- **(b) 인위적 극단값** — `-1` 이 14.81% 였다. 사실상 "평가 이력 없음" 이진 신호가
  점수 축의 극단값 형태로 들어가 있었다.

(a)는 제거해야 할 누수이고, (b)는 정당한 신호를 잘못된 형태로 담고 있던 것이다.
STAGE 5 에서 `-1` 을 NaN + `CG01_MISSING_YN` 으로 바꿨으므로 (b)는 올바른 형태로 살아난다.

| 시나리오 | 구성 | 해석 |
|---|---|---|
| S2a | `CG01_KIS_SCORE` 제거 (기존 S2) | 기준 |
| S2b | 유지 + `-1` → NaN/MISSING_YN 전환만 적용 | gain 급락 시 = 기존 중요도는 `-1` 아티팩트 |
| S2c | `CG01_MISSING_YN` 만 유지, 점수 자체는 제거 | S2b 와 비슷하면 = "평가 이력 유무" 가 신호 |

세 버전의 AUC 와 해당 변수군 gain 을 비교한다.
**동일 분석을 `C302_CRI_ORD` 에도 적용한다** (같은 `-1` 대체 대상이었다).

이 비교 결과는 STAGE 7 최종 보고서에 별도 섹션으로 넣는다.
"중요 변수의 높은 중요도가 실제 신호인지 전처리 아티팩트인지 분리 검증" 은
방법론적 엄밀성을 보여주는 소재다.


---

# STAGE 6 선행작업 — 처리 완료 (2026-08-30)

`STAGE6_PREFLIGHT.md` / `step30_scenario_plan.md` 참조.

| # | 작업 | 상태 |
|---|---|---|
| 1 | `portal_v2.duckdb` 재빌드 | 완료. 948,214행 / 268컬럼. `portal.duckdb` 는 부재했고 손대지 않음 |
| 2 | `db_migration.py` / `init_duckdb.py` 갱신 | 대체. 구 스크립트는 `os.remove(portal.duckdb)` 를 하므로 STAGE 6 경로에서 제외하고 `database/build_portal_v2.py` 신규 작성 |
| 3 | `step7` 경로를 `config.read_panel()` 로 | 완료 |
| 4 | `validation_common.py` 하드코딩 절대경로 4건 | 완료. 전부 config 경유, DB 연결은 예외 없이 `read_only=True` |
| 5 | `step7` 피처 선정 로직 | 완료. `leaky_cols.feature_columns()` 단일 경로 + assert |
| 6 | `step7` early stopping Dev셋 | 완료. `eval_set=[(X_dev, y_dev)]` |

## 새로 드러난 것

**LEAK_CONFIRMED 10개가 `portal_v2.duckdb` 에 없다.** step5 가 떨어뜨렸다.
assert 는 통과하지만 "제외했다"가 아니라 "들어온 적이 없다"는 뜻이다.
Ablation A1/A2(누수 변수 되넣기)는 step5 이전 산출물
`nh_panel_full_obv.parquet` 에서 조인해 복원한다 (매칭률 100.0000%).

**`CG01_MISSING_YN` / `C302_MISSING_YN` 이 `LEAK_SUSPECT` 에 없어 기본 피처에 포함된다.**
제안 58 의 아티팩트 분리 검증(A3/A4, A5/A6)이 성립하려면 기준선에서 빼야 한다.
`step30_scenario_plan.md` §1 ★ 참조.

**무분산 컬럼 4개**: `JEMU_debt_dependency_capped`, `JEMU_asset_turnover_end_capped`,
`JEMU_asset_turnover_avg_capped`, `HAS_OBV_YN` — 패널 전체에서 값이 1개뿐이다.
파생 계산이 전부 0으로 떨어진 것으로 보이며 별도 확인이 필요하다.

**`IS_BUDO_12M` 양성 9,814** — STAGE 5 기대치 9,985 와 171건 차이. **해결(A-2)**: 9,985 는 step5 `drop_in_default_periods` 적용 전(951,908행) 값, 9,814 는 적용 후(948,214행) 값이다. 정본은 9,814. 근거 `A2_number_reconciliation.md`.

## 환경 메모

이 PC 는 ML 스택이 없었다(`duckdb`/`pandas` 만). `C:/Users/scudy/.venvs/nh_eco` 에
venv 를 만들고 lightgbm 4.7.0 / scikit-learn 1.9.0 / shap 0.52.0 을 설치했다.
OneDrive 동기화를 피해 프로젝트 밖에 두었다.

프로젝트 경로에 한글이 있어(`바탕 화면`, `NH AI경진대회`) LightGBM 의 C++ 파일 IO 가
`LightGBMError: Could not open ...` 로 죽는다. **모델 저장/로드는 반드시
`config.load_booster()` / `config.save_booster()` 를 쓸 것.**

---

# [2026-09-01] 2차 매핑 사고 — 거시 지표 항목코드 오매핑

JEMU 라벨 한 칸 밀림에 이은 **두 번째 매핑 사고**다. 이번에는 라벨이 아니라
**수집된 데이터 자체가 다른 계열**이었다.

- 감사 보고서: `ECOS_MAPPING_AUDIT.md` (전 소스), `ECOS_ITEM_CODE_AUDIT.md` (817Y002 상세)
- 감사 스크립트: `api_data_processing/audit_mapping.py` (라이브 조회, 재실행 가능)
- 결과: **ECOS 44개 중 불일치 28 / YAHOO 22개 중 불일치 1 / KOSIS 3개 중 불일치 1**

## 재검증 대상

| # | 대상 | 상태 | 비고 |
|---|---|---|---|
| 1 | `indicators.csv` 항목코드 정정 | **완료 (2026-09-01)** | 23건 정정. `old_stat_code`/`old_item_code1`/`old_item_code2` 에 기존 값 보존. 재감사 ECOS 일치 39/44, 잔여 5건은 (d)·enabled=N |
| 1b | 전체 재수집 (`output/` → `output_pre_remap/` 보존) | **완료 (2026-09-01)** — 65/65 성공 | 정정 23건만이 아니라 전체 65개를 다시 받았다 |
| 1c | KOSIS `construction_cost_index` 차원 필터 | **완료 (2026-09-01)** | `C1_NM='건설'`(총지수) 필터 추가. 기존은 '기타건설' 이 저장되고 있었다 |
| 2 | 파생 `credit_spread` / `liquidity_spread` | 대기 | credit_spread 무효, liquidity_spread 는 정상 확인 |
| 3 | Phase 6 산출물 3개 재생성 | 대기 | `LV_credit_spread` / `CUM_spread_stress_12m` / `PCT_spread_5y`. 나머지 9개 유지 |
| 4 | 172개 산출물 중 `credit_spread_diff12` 등 파생 | 대기 | |
| 5 | **D축 §3 · §5 서술** | **정정 주석 완료 (보강 2026-09-01)** | `D_AXIS_RESULT.md` 상단 및 해당 절. 수치는 미변경 |
| 6 | D축 전체 재실행 여부 | **판단 대기** | 불일치 28건 → 사전 기준상 "전면 재매핑" 구간 |
| 7 | E0 가설 검증 재실행 (수준 vs 차분 부호) | 대기 | credit_spread 계열 부호가 바뀔 수 있다 |
| 8 | E2 착수 | **보류** | 정정·재수집 완료 전까지 시작하지 않는다 |

## D축 결론의 유효 범위

- **유지**: "거시 결합이 캘리브레이션을 악화시켰다" — 잘못된 데이터로도 실패했다는 사실은 불변.
- **정정**: §3 의 "금리 경로는 credit_spread_x_lev 가 커버한다" 는 논거 무효
  (신용스프레드가 아니라 국고채 1년−10년 기간 스프레드였다).
- **정정**: §5-1 의 `CPI_core_yoy` 는 물가가 아니라 주택매매가격지수(KB) 상승률.

## 재발 방지 (수집 단계 내장)

`data_collector.py` 에 항목명 대조 · 값 범위 · 파생 부호 검증을 넣었다.
`indicators.csv` 에 `expected_name_kr` 컬럼을 추가해 의도한 계열명을 명시한다.
**검증 코드는 지금 만들되 실행은 재수집 시점에 한다.**

## 미결 — (d) 지표 부재 3건 + enabled=N 3건

정정하지 않고 별도 승인을 기다린다. **임의 대체 금지.**

| series_name | 사유 | 상태 |
|---|---|---|
| `manufacturing_index` | 의도한 제조업 생산지수의 총계 항목이 ECOS 월별에 없다 | 판단 2 보고 완료, 결정 대기 |
| `trade_total` | 정의 미확정. 정정 후 `import_index` 와 완전 중복이 된다 | 판단 2 보고 완료, 결정 대기 |
| `US_2Y_treasury` | `^IRX` 는 13주 T-bill. Yahoo 에 안정적 2년물 티커 없음 | 별도 판단 대기 |
| `GNI_annual` | Group1(구분코드)만 지정하고 Group2(계정항목) 미지정 — 차원 구조 오류 | enabled=N 유지. 되살릴 때 재설계 |
| `GNI_nominal` | Group2 코드를 `item_code1`(Group1) 자리에 넣은 차원 자리 오류 | enabled=N 유지 |
| `GNI_per_capita` | 위와 동일 | enabled=N 유지 |

GNI 3건은 **연간 지표라 월 패널에서 (기업,연도) 내 변동이 0 이 되는 시점 누수 구조**라는
기존 사유(2026-08-30 주석)로 이미 `enabled=N` 이다. 항목코드 오류는 그 위에 겹친 별개 결함이며,
되살리는 결정이 나기 전까지 정정하지 않는다.

## E2 시나리오 후보 — 정정으로 새로 생긴 것 (2026-09-01 기록)

`export_price_index_KOR` 는 구 매핑에서 **주요국 경제성장률(연간)** 이었으나
정정 후 **402Y014 수출물가지수(기본분류) 총지수(월별)** 가 되었다.
드롭 사유(대량 결측)가 소멸해 `DROP_COLS` 에서 제외했고 Group C(+2) → Group B(+1) 로 옮겼다.

- **상호작용 후보**: `export_price_index_KOR` × `exp_fx`(수출비중).
  D축의 `fx_shock_x_export`(환율충격 × 수출비중, gain 0.000 / 255위, 충격0 3개월) 와
  **같은 수출 경로를 가격 측면에서 잡는 항**이다. E2 시나리오 검토 시 둘을 함께 볼 것.
- 환율 충격은 수출기업의 원화 환산 매출을, 수출물가는 계약 단가를 각각 움직인다.
  `fx_shock_x_export` 가 gain 0 에 가까웠던 원인이 충격 정의에 있었는지
  노출도(exp_fx)에 있었는지 이 항과 대조하면 갈린다.
- **선행 조건**: 재수집 후 `export_price_index_KOR` 실제 결측률 확인. **5% 초과 시 재보고.**

## KOSIS 세부항목 필터 — 3건 전수 재확인 (2026-09-01)

`construction_cost_index` 의 필터 부재가 발견되어 나머지 2건도 같은 기준으로 확인했다.

| series_name | 차원 구성 | 적용 필터 | 필터 전 → 후 (2024년) | 판정 |
|---|---|---|---|---|
| `construction_cost_index` | C1 = 공사 부문 24종 | `C1_NM='건설'` (총지수) | 288 → 12 | **정정 완료** (기존은 '기타건설') |
| `unemployment_rate` | C1 = 성별 3종, C2 = 연령 20종 | `C1_NM='계'`, `C2_NM='계'` | 720 → 12 | 정상 |
| `unsold_housing` | C1 = 지역 19종, C2 = 부문 3종, C3 = 규모 7종 | `C1_NM='전국'`, `C2_NM='총합'`, `C3_NM='총합'` | 1,596 → 12 | 정상 |

- 세 건 모두 월당 정확히 1행으로 수렴한다 (`groupby(date).last()` 가 임의 선택할 여지 없음).
- 실측 대조: 2024-12 미분양 70,173호 / 2024년 실업률 1.9~3.8% — 공표치와 일치.
- **잔여 주의**: `unemployment_rate` 는 **원계열(계절조정 아님)** 이다.
  1~2월 높고 8월 낮은 계절성이 그대로 들어온다. 매핑 오류는 아니나
  모델에서 계절성을 흡수하지 않으면 왜곡 요인이다. 계절조정 계열이 필요하면 별도 통계표를 찾아야 한다.

## 결정 확정 (2026-09-01)

| 건 | 결정 | 적용 |
|---|---|---|
| `export_price_index_KOR` 통화기준 | **W(원화기준) 유지** | `item_code2='W'`. 정정 단계에서 지표를 함께 바꾸면 원인 분해가 불가능해진다 |
| `trade_total` | **(a) enabled=N 드롭** | `import_index`(403Y003/*AA) 와 100% 중복 |
| `US_2Y_treasury` | **(b) `US_3M_tbill` 로 개명** | ticker `^IRX` 유지. 실제 계열은 13주 T-bill |
| `manufacturing_index` | **(x) 의도적 드롭** | `DROP_COLS` 유지 |
| GNI 3건 | **(x) 의도적 드롭** | 시점 누수 사유로 이미 확정 |

- **E2 착수 시 C(계약통화기준) 계열 추가 수집 검토.** `402Y014` Group2 는 D(달러)/W(원화)/C(계약통화)
  3종이다. `xprice_x_export` 를 만들 때 W 와 C 를 **둘 다 두고 비교**한다. 지금 하나로 갈아끼우면
  비교 대상이 사라진다.
- `US_10Y_treasury`(^TNX) − `US_3M_tbill`(^IRX) = **미국 기간스프레드(10Y−3M)** 로 쓸 수 있다.
  D축에서 `credit_spread` 가 사실은 기간스프레드였던 것과 대비된다.
  **지금 파생으로 만들지 않는다** — 지표명이 정확해졌다는 사실만 기록한다.

### 개명·드롭의 잔여 참조 (재학습 전까지 손대지 않음)

| 위치 | 내용 | 조치 시점 |
|---|---|---|
| `eda_pipeline/output/lgbm_12m_model.txt` | `feature_names` 에 `US_2Y_treasury_diff12` 가 학습된 채로 박혀 있다 | **해소 (2026-09-03).** `backend/model_inference.py` 가 `lgbm_v2_lean_macro.txt`(59피처)로 교체되어 이 파일은 더 이상 서빙에 쓰이지 않는다 |
| `eda_pipeline/output/lgbm_12m_lean_model.txt` | 동일 | 재학습 시 |
| `backend/model_inference.py` `_GROWTH_COLS` | `'trade_total_yoy'` 가 남아 있다 | **재학습 후 제거**. `shift()` 가 `if c in shocked.columns` 로 막아 주므로 런타임 오류는 없다 |
| `output/` 하위 생성물 (`macro_columns_v2.json`, `nh_panel_*.csv`, `standardized/` 등) | 구 지표명 포함 | 3~5단계 재생성으로 해소 |

## 공표 지연 실측 (2026-09-01) — Group B 시차 재검토 대상 6건

`impute_data.py` 의 시차 배정이 실제 공표 시점과 맞는지 **ECOS 실데이터의 최종 수록월**로 측정했다.
ECOS 통계표 메타에는 공표시점 필드가 없고 한국은행 공표일정 페이지도 조회되지 않아,
**실데이터 가용 시점을 직접 재는 방법**을 썼다. 측정일 하루치이므로 관측 방법을 함께 남긴다.

| series_name | 현재 | 제안 | 실데이터 최종월 | 경과 | 근거 |
|---|---|---|---|---:|---|
| `current_account` | B(+1) | **C(+2)** | 2026-06 | 3개월 | 9/1 시점에 7월 값 미공표 → t+1 월말까지 안 나온다 |
| `goods_balance` | B(+1) | **C(+2)** | 2026-06 | 3개월 | 동일 (국제수지 익익월 공표) |
| `M1_narrow_money` | B(+1) | **C(+2)** | 2026-06 | 3개월 | 통계표가 161Y001 로 바뀌며 지연이 길어졌다 |
| `M2_broad_money` | B(+1) | **C(+2)** | 2026-06 | 3개월 | 161Y005 |
| `Lf_liquidity` | B(+1) | **C(+2)** | 2026-06 | 3개월 | 171Y003 |
| `monetary_base_sa` | B(+1) | **C(+2)** | 2026-06 | 3개월 | 기존부터 있던 문제 (102Y004 미변경) |

**미적용 — 승인 대기.** 시차 연장은 안전한 방향이나 지시 없이 바꾸지 않는다.

### 별건 관찰 — 분기 지표 3건은 +2 로도 부족할 수 있다  → **해소 (2026-09-02)**

`current_account_quarterly` / `household_credit` / `household_loan` 은 Group C(+2) 인데
실데이터 최종이 2026-06(3개월 경과)이었다. **Group D(+3) 를 신설해 세 건을 옮겼다.**
아래 `[2026-09-02]` 절 참조.

> 이 절의 초판은 "분기 지표라 월 단위 시차 해석이 다르다" 를 근거로 판단을 보류했다.
> 그 근거는 **틀렸다.** 분기 지표는 (기업,연도) 안에서 4회 변하고 `ffill` 은 과거 값을
> 반복하므로 `GNI_annual` 유형의 look-ahead 구조가 아니다. "연 4회 이하 = GNI 유형" 이라는
> 기준 자체가 무효다 (그 기준이면 평균 3.2회 변하는 `base_rate` 도 함께 걸린다).
> 조치 근거는 **관측된 공표 지연 3개월** 하나여야 한다.

# [2026-09-01] 3~5단계 완료 — 재수집·재생성·E0 재검증 결과

## 3단계 재수집

- `api_data_processing/output/` → **`output_pre_remap/`** 로 보존 (삭제 없음, 6.1MB)
- 전체 재수집: **65/65 성공, 실패 0건** (enabled=Y 65개)
- 산출: `model_input_monthly.csv` 77행 × 66컬럼 / `model_input_daily.csv` 1,673행 × 66컬럼

## 4단계 검증 5종

| 검증 | 결과 |
|---|---|
| 수집 실패 목록 | **0건** |
| 차원 미지정(계열 혼입) | **1건** — `manufacturing_index`(501Y013, 205계열). 드롭 대상이라 예상된 경고. **세 번째 사례는 없다** |
| ITEM_NAME 대조 | **1건** — `manufacturing_index` (동일 사유) |
| 값 범위 sanity | **2건** — `manufacturing_index` 기준연도 / `BSI_mfg_biz` 49 (기준 50~150의 1p 경계) |
| 파생 스프레드 부호 | **통과** (아래 5단계 참조) |

### 가드 자체의 결함을 한 번 잡았다

초판 실행에서 **공표 지연 경고가 62건** 났다. `--end-date 2026-05-31` 로 끊어 받아 놓고
기준을 '오늘'(2026-09-01)로 잡아 전 지표가 4개월 지연으로 잡힌 오탐이었다.
기준을 **요청 종료일과 오늘 중 이른 쪽**으로 고쳤다. 정정 후 경고는
`manufacturing_index` 1건뿐이다.

함께 고친 2건:
- KOSIS 3건이 응답 명칭을 남기지 않아 항목명 대조가 늘 실패했다 → `PublicDataCollector.last_meta` 추가
- `WTI_crude_oil` 2020-04-20 −37.63 은 실측 사건이므로 PRICE 양수 규칙의 예외로 명시

## 5단계 재생성 — 필수 확인 4가지

### (a) `LV_credit_spread` 부호 — **해소**

| | 정정 전 | 정정 후 |
|---|---|---|
| 음수 개월 | **68 / 77 (88.3%)** | **0 / 77 (0%)** |
| min / 중앙 / max | −1.546 / −0.567 / +0.251 | **+0.406 / +0.677 / +1.756** |
| Phase 6 `LV_credit_spread` | — | 65개월 중 음수 **0** |

### (b) 2022-10 (레고랜드) 확대 — **신용스프레드의 특징이 나타난다**

| 계열 | 수집값 | 시장 실측 |
|---|---:|---|
| 회사채 AA- 3년 | **5.580** | ≈5.5 |
| 국고채 3년 | **4.185** | ≈4.2 |
| 스프레드 | **+1.395 (140bp)** | ≈130bp |

평시(2021 평균) +0.673 → 위기(2022-10) **+1.395** 로 2배 확대. 위기 시 확대·평시 축소가 확인된다.
미 기간스프레드(10Y−3M)와의 상관은 **−0.904 → +0.014** 로 소멸했다 — 더는 기간 요인이 아니다.

### (c) ★ E0 가설 — **정정 데이터에서도 성립하지 않는다**

E0-1 수준 8개 중 **유지 4 / 뒤집힘 4.**

| 지표 | 정정 전 | 정정 후 | |
|---|---|---|---|
| `credit_spread` 수준 | ★뒤집힘 +0.865/−0.585 | **유지 +0.356/+0.281** | ← 변화 |
| `credit_spread_diff12` | ★뒤집힘 +0.755/−0.454 | **유지 +0.462/+0.861** | ← 변화 |
| `CPI_core` 수준 | 유지(약함) −0.006/−0.174 | 유지 **+0.978/+0.910** | 강해짐 |
| `base_rate` 수준 | ★뒤집힘 +0.977/−0.918 | ★뒤집힘 (동일) | |
| `treasury_bond_3y` 수준 | ★뒤집힘 | ★뒤집힘 +0.849/−0.823 | |
| `corporate_bond_3y_AA` 수준 | ★뒤집힘 | ★뒤집힘 +0.823/−0.798 | |
| `liquidity_spread` 수준 | ★뒤집힘 | ★뒤집힘 (동일) | |
| `USD_KRW` / `BSI_mfg_biz` 수준 | 유지 | 유지 (동일) | |

**정정으로 `credit_spread` 계열은 살아났지만, 금리 수준 계열(`base_rate`·국고채·회사채·
유동성스프레드)의 부호 반전은 그대로다.** "수준값이면 부호가 안 뒤집힌다"는 전제는
데이터 오류 때문이 아니었다. **E1 부터 재설계가 필요하다.**

### (d) D축 gain 상위 10 중 정정 대상 — **5 / 10 (절반)**

| 순위 | 변수 | gain% | 기반 원지표 | 정정 |
|---:|---|---:|---|---|
| 1 | `rate_shock_x_leverage` | 0.888 | base_rate | — |
| 2 | `CPI_core_yoy` | 0.150 | CPI_core | **★** |
| 3 | `credit_spread_x_lev` | 0.148 | corporate_bond_3y_AA, treasury_bond_3y | **★** |
| 4 | `housing_price_index_yoy` | 0.122 | housing_price_index | **★** |
| 5 | `import_index_yoy_ma3m` | 0.077 | import_index | **★** |
| 6~9 | corn / BSI_nonmfg ×2 / soybean | 0.073~0.040 | — | — |
| 10 | `trade_total_yoy` | 0.036 | trade_total | **★ (드롭되어 소멸)** |

사전 기준 "절반 이상이면 D축 재실행" 충족 → **D축 재실행 대상.**

## 파생 변수 개수

`model_input_monthly_cleaned.csv` **178개 → 178개** (순증 0).
- 추가 4: `US_3M_tbill_diff12(_ma3m)`, `export_price_index_KOR_yoy(_ma3m)`
- 제거 4: `US_2Y_treasury_diff12(_ma3m)`, `trade_total_yoy(_ma3m)`

> 문서의 "172개"는 그 이전 세대 수치다. 정정 **전** 산출물도 이미 178개였으므로
> 이번 정정이 개수를 바꾼 것이 아니다. `data_preprocessing.md` 의 172 표기는 별도 정정 대상이다.
> 원지표 63개(enabled=Y 65 − DROP_COLS 2)는 예상과 일치한다.

---

# [2026-09-02] 시차 그룹 D 신설 · 가드 임계 조정 · AC12 정합화 결함 · D축 재실행

## 1. 분기 3건 Group C(+2) → Group D(+3)

| 지표 | 이동 | 실데이터 최종월 | 경과 | 상태 |
|---|---|---|---:|---|
| `household_credit` | C → **D** | 2026-06 | 3개월 | **완료** |
| `household_loan` | C → **D** | 2026-06 | 3개월 | **완료** |
| `current_account_quarterly` | C → **D** | 2026-06 | 3개월 | **완료** |

- 근거는 **실데이터 최종월 관측(측정일 2026-09-01)** 이다. 2026-09-02 재측정도 동일했다.
  Group C(+2) 는 t 월 값을 t+2 월에 쓰는데 실제 공표는 t+3 이므로 1개월 look-ahead 가 남았다.
- **드롭하지 않았다.** 시차만 늘리면 look-ahead 는 해소되고 정보는 남는다.
- **구조 선택**: 지표별 개별 시차 대신 **Group D 신설**을 택했다. 코드 변경이 훨씬 적다 —
  `GROUP_D_COLS` / `LAG_MONTHS_D=3` 추가와 `phase0/1/3` 인자 하나, 그리고
  `data_collector.SHIFT_GROUP` 에 `"D": 3` 뿐이다. 지표별 시차는 그룹 배정 검증
  (`phase0` 의 미배정 예외, `_verify_against_config`)과 `phase3` 의 변환 대상 산정을
  전부 다시 짜야 한다.
- **산출물 영향**: `household_credit_yoy` · `household_loan_yoy` ·
  `current_account_quarterly_yoy` 와 각각의 `_ma3m` = **6개 컬럼의 값만** 1개월 뒤로 밀렸다.
  컬럼 집합과 총 개수(178)는 불변. `_yoy` 블록 안에서 세 컬럼의 **순서**만 뒤로 옮겨졌다
  (하류는 컬럼명 참조이므로 무영향).

## 2. 가드 임계 `lag > shift + 1` → `lag > shift` + 전 지표 재검사

구 임계는 "경과월 = 시차 + 1" 인 **경계선을 통과시켰다.** 그 경계선이 곧 1개월
look-ahead 다. 분기 3건이 Group C(+2) 에 경과 3개월로 앉아 있던 것이 그 증거다.

재검사 스크립트를 새로 만들었다 — `api_data_processing/audit_publication_lag.py`.
수집 산출물을 건드리지 않고 각 지표의 **실데이터 최종 수록월만** 조회해 잰다
(`indicator_metadata.csv` 의 `data_end` 는 수집 요청 종료일 2026-05-31 에 묶여 있어
공표 지연 측정에 쓸 수 없다). 리포트: `PUBLICATION_LAG_AUDIT.md`.

**새로 걸린 15건 (전부 `경과 = 시차 + 1` 경계선)**

| 그룹 | 지표 | 최종 수록월 | 경과 |
|---|---|---|---:|
| B(+1) | `CPI_core`, `CPI_core_excl_food_energy`, `CPI_food_nonalcohol`, `PPI_total`, `export_price_index_KOR`, `export_index`, `import_index` | 2026-07 | 2 |
| C(+2) | `M1_narrow_money`, `M2_broad_money`, `Lf_liquidity`, `monetary_base_sa`, `current_account`, `goods_balance` | 2026-06 | 3 |
| C(+2) | `unsold_housing`, `construction_cost_index` | 2026-06 | 3 |

- Group A 35건 0 / Group D 3건 0. 조회 실패 1건 = `manufacturing_index`
  (차원 구조 오류로 `DROP_COLS` 유지 중인 지표. 예상된 실패다).
- **15건은 시차를 바꾸지 않았다.** 판단 근거가 부족하다:
  측정일이 **월초**(9/1, 9/2)라 월간 통계의 익월 중순 공표를 아직 못 본 상태일 수 있다.
  `shift(+1)` 이 묻는 것은 "8월 값을 **9월 안에** 쓸 수 있는가" 이고, 9월 중순 공표라면
  답은 예다. 9/1 과 9/2 결과가 같았으므로 하루 차이로 갈리는 문제도 아니다.
  **공표일까지 관측한 뒤 결정할 사안이다.**
- Group D 3건은 다르다 — 분기 지표의 경과 3개월은 월초 효과로 설명되지 않는다.

| # | 필요 조치 | 상태 |
|---|---|---|
| 11 | 경계선 15건의 실제 공표일 관측 후 시차 재판정 | **대기** — 월 중순 이후 `audit_publication_lag.py` 재실행 |

## 3. 실행 환경 통일 — venv + pandas 교차 검증

### ★ 정정 — 버전 방향이 지시서와 반대였다

| | 지시서 가정 | **실측** |
|---|---|---|
| venv `C:/Users/scudy/.venvs/nh_eco` | pandas 2.3.3 | **pandas 3.0.5** / numpy 2.5.2 / Python 3.14.3 |
| 시스템 Python 3.14 | pandas 3.0.5 | **pandas 2.3.3** / numpy 2.4.4 |

- venv 는 Python **3.14.3** 이며 lightgbm 4.7.0 / scikit-learn 1.9.0 / scipy 1.18.1 이
  정상 설치돼 있다. "Python 3.14 는 lightgbm 휠이 없을 것" 이라는 우려는 이 환경에서는
  해당하지 않는다.
- 시스템 Python 에는 **lightgbm·scikit-learn·scipy 가 없다.** 학습은 애초에 venv 에서만 된다.
- 두 환경 모두 pyarrow·fastparquet 이 없다. parquet 는 duckdb 로 읽고 쓴다.

### 교차 검증 결과 — 조용한 데이터 손상 없음

두 인터프리터에서 같은 파일을 읽어 `shape` / 컬럼 순서 / 컬럼별 dtype · 결측수 ·
`sum` · `min` · `max` (문자열·범주형은 고유값 전수)를 대조했다.

| 파일 | shape | 값 불일치 | dtype 불일치 |
|---|---|---:|---|
| `model_input_monthly_cleaned.csv` | 65 × 179 동일 | **0** | 0 |
| `model_input_monthly_level.csv` | 65 × 14 동일 | **0** | 1 (`date`) |
| `nh_panel_macro_12m_obv_none_real.parquet` | 948,214 × 267 동일 | **0** | 10 |
| `nh_panel_B46_asof.parquet` | 948,214 × 173 동일 | **0** | 8 |

- 불일치는 **문자열 dtype 표기뿐**이다 — pandas 3.0 은 새 기본값 `str`, 2.3.3 은 `object`.
  `exp_fx_industry_level` · `exp_fx_source` · `STD_INDS_SECTION` · `STD_INDS_MID2` 의
  고유값 집합도 완전히 같다. 학습 코드가 `astype("category")` 로 변환하므로 모델에는
  표기 차이가 전달되지 않는다.
- 그래도 **앞으로 모든 실행을 venv 로 통일한다.** 이번 재실행(impute_data / step6 /
  step32 / step31 / step34)은 전부 venv 로 돌렸다.
  재현 방법과 패키지 버전은 `api_data_processing/data_preprocessing.md` 의
  "재현 방법" 절에 명시했다.

## 4. ★ 신규 발견 — AC12 정합화(B6/B46)가 작동하지 않고 있었다

`step32_panel_variants.rebuild_ac12_asof` 가 원천의 **물리 컬럼명**
(`FC_AM1`, `LA_INSP_KRW_AM1` …)을 `step1_load.AC12_RENAME` 로 논리명
(`US_FC_AM`, `US_KRW_AM` …)으로 바꾸지 않고 그대로 `AC12_` 접두사를 붙였다.

결과: `AC12_FC_AM1` 같은 **새 컬럼 10개가 추가되기만** 하고, 패널의 실제 AC12 피처
(`AC12_US_FC_AM` … `AC12_TOTAL_KRW_AM`)는 **연 단위 조인분 그대로** 남았다.
`exp_fx_dbt` 재계산도 그 낡은 값으로 이뤄졌다.

- **발견 경로**: step31 재판정에서 B46 패널의 AC12 10개가 여전히 (기업,연도) 내 변동 0 으로
  나왔다. 교정했다면 나올 수 없는 값이다.
- **영향**: 기존 `B46` / `B6` 측정의 "AC12 정합화 후 성능 동일" 은 **아무것도 바꾸지 않은
  패널을 재서 얻은 결과**다. 근거로 쓸 수 없다. → 재측정 대상.
- **수정**: `AC12_RENAME` 과 `EXT_OTHER_KRW_AM` 파생을 `step1_load` 에서 임포트해 적용했다
  (사본을 두면 두 곳이 갈라진다). 같은 사고가 조용히 반복되지 않도록,
  **패널의 AC12 컬럼을 하나라도 덮어쓰지 못하면 예외로 중단**하는 가드를 넣었다.
- **결측 규칙**: as-of 매칭이 없거나 이월 상한(12개월)을 넘긴 행은 `step5._split_missing` 과
  같이 **0** 으로 둔다 (유형 1 = 구조적 부재). NaN 으로 두면 `exp_fx_dbt` 결측률이
  5.28% → 93.99% 로 뛰어 B6 의 ΔAUC 가 '시점 교정 효과' 와 '0→NaN 의미 변경 효과' 를
  섞어 버린다. 재는 것은 시점 하나여야 한다. "레코드 없음/오래됨" 정보는
  `HAS_AC12_YN` 과 `AC12_STALE_MONTHS` 가 따로 담는다.

## 5. step31 시점 누수 재판정 — B46 교정 패널

진단기 자체도 고쳤다. `_leaky_ancestors` 가 `KNOWN_LEAK`(과거 관측 목록)만 보고
(d) 부분오염을 판정하고 있었다. 그러면 교정된 패널을 감사해도 조상이 영구히 누수로 남아
**교정 효과를 이 진단으로 볼 수 없다.** 조상의 누수 여부도 **그 패널에서 실측한 변동 건수**로
판정하도록 바꿨다. `--panel` 로 임의 parquet 을 감사할 수 있게도 했다.

| 분류 | portal_v2 (교정 전) | B46 (교정 후) |
|---|---:|---:|
| 정합 | 143 | 153 |
| **(b) 시점누수** | **11** | **0** |
| (c) 시불변 | 6 | 6 |
| (d) 부분오염 | **2** | **0** |
| 무분산 | 4 | 0 |

- 해소된 (b) 11개 = `AC12_US_FC_AM` · `AC12_US_KRW_AM` · `AC12_JP_FC_AM` ·
  `AC12_JP_KRW_AM` · `AC12_CN_FC_AM` · `AC12_CN_KRW_AM` · `AC12_EU_FC_AM` ·
  `AC12_EU_KRW_AM` · `AC12_TOTAL_KRW_AM` · `AC12_EXT_OTHER_KRW_AM` · `HAS_AC12_YN`.
- 해소된 (d) 2개 중 `exp_fx_dbt` 는 B46 에서 해소됐다.
- **★ `fx_vol_x_fxdebt` 는 아직 해소되지 않았다.** 이 항은 거시 상호작용이라 B46(A0 기반)
  패널에 없고, D축 패널(`step5 → step6`)에 있다. 그 패널은 **연 단위 조인분 AC12** 위에서
  `exp_fx_dbt` 를 만든다. 즉 D축의 `exp_fx_dbt` 와 `fx_vol_x_fxdebt` 는 여전히 (d) 다.
- 같은 이유로 **D0 의 기업 고유 피처에 들어가는 `exp_fx_dbt` 도 (d) 상태**다.
  D6 의 3개 항은 AC12 를 쓰지 않으므로 D6 판정 자체는 이 오염과 무관하다.

| # | 필요 조치 | 상태 |
|---|---|---|
| 12 | AC12 월 단위 as-of 를 `step2`(또는 `step5`) 본류에 반영 → D축 패널의 `exp_fx_dbt` · `fx_vol_x_fxdebt` (d) 해소 | **대기 — 승인 필요.** 본류 변경이라 A/B/C/D 전 축의 기준선이 함께 움직인다 |
| 13 | `B46` / `B6` 재측정 (4번 수정 반영) | 아래 6번 |

## 6. CRIF LEAK_CONFIRMED 유지 — 근거 기록

**이번 작업 최고의 증거다.** 같은 4개 변수군을 조인 방식만 바꿔 두 번 측정했다.

| 측정 | 조인 | A0 대비 ΔAUC | 판정 |
|---|---|---:|---|
| `A2` | (V_BZNO, **연도**) — `CRDBD_OCU_YY` 를 `str[:4]` 로 절단 | **+0.0679** | 원 모델 gain 36.34% (1위) |
| `B4f` | (V_BZNO, **월**) as-of — `BASE_YM` 이전 발생분만 | **+0.0018** | 노이즈 규칙 `|ΔAUC| < 0.003` 이내 |

- 원본 gain 36.34%(1위) → 시점 정합화 후 기여 **+0.0018(노이즈)**.
  **그 기여가 전부 누수였다**는 것을 정면으로 증명한다.
- **`LEAK_CONFIRMED` 유지**. 정합화해도 기여가 노이즈면 되살릴 실익이 없다.
  다만 "정합화 시도 후 기여 +0.0018" 을 **기록으로 남긴다** — 이것이 근거다.
- `04_누수_발견과_제거` 의 핵심 자료로 쓸 것.
- **`CRIF_CNT_EVER` 제거 (승인)**: `CRIF_CNT_12M` 과 비영 비율이 동일하고 값이 다른 행이
  3개뿐이었다. 지시서 명세 4종 → **3종**(`CRIF_CNT_12M`, `CRIF_MONTHS_SINCE`,
  `CRIF_WORST_RSNC`). '과거에 있었다' 는 `CRIF_MONTHS_SINCE` 가 담는다.
- `RAW_CRIF` NameError 수정 완료 (B4 가 구현만 되고 실행된 적이 없어 상수가 빠져 있었다).
- `JEMU_capital_impaired` 는 **값 생성 오류가 아님을 확인**했다. 15,366행 완전 일치이고
  원변수 `JEMU_118900`(gain 1.200%, 18위)이 같은 조건을 담아 직접 분기하므로 플래그의
  gain 0 은 정상이다. 기존 판정 유효.

> ★ 위 A2 / B4f 수치(+0.0679 / +0.0018)는 **AC12 결함 수정 이전** 패널에서, 그리고
> **서로 다른 세대의 A0 기준선**(0.7583)에 대해 잰 것이다. 아래 8번에서 한 세대 안에서
> 다시 쟀다. 결론은 같고, 수치는 더 깨끗해졌다.

## 7. D축 재실행 — 정정 거시 + Group D + D6

선행 재생성: `impute_data`(Group D 반영) → `step6_macro_integration --tag real`.
새 패널은 **948,214 × 265** 다 (구 267 에서 `US_2Y_treasury_diff12` ·
`trade_total_yoy` 2개 소멸 — 구 패널이 매핑 정정 이전 세대였다).
거시 축소 후 89개.

### 게이트 1 — 8/8 통과 (단, 진단기 결함 1건을 먼저 고쳐야 했다)

첫 재산출에서 **7/8** 이 나왔다. G1-7(그룹별 실제 총 시차)이 실패했는데
**데이터가 아니라 프로브 기대값이 낡은 것**이었다 — `M2_broad_money` 를
Group B(+1) 로 박아 둔 채였고, 2026-09-01 에 Group C(+2) 로 옮긴 것이 반영되지
않았다. 실측 시차 2개월은 옳은 값이었다.

- 조치: 기대 시차를 `impute_data` 의 `GROUP_*_COLS` 에서 **파생**하도록 바꿨다.
  시차 배정의 정본은 `impute_data` 하나여야 한다. 하드코딩을 없앴다.
- Group D 프로브 2건(`household_credit_yoy`, `current_account_quarterly_yoy`)을
  신설했다. **실측 시차 3개월 = 기대 3개월** — Group D 이동이 실제로 적용됐음을
  독립 검증한다.
- 조정 후 **8/8 통과**.

### 결과 (규제 R2 · 시드 3회 42/7/2024 · 주보정 `platt_train`)

| ID | 구성 | 피처 | Valid AUC(σ) | ΔAUC | 거시gain% | PD-부도율 상관(σ) | 건수 MAPE(σ) | Brier | ECE | 판정 |
|---|---|---:|---|---:|---:|---|---|---:|---:|:---:|
| D0 | 기업 고유 + 노출도만 | 156 | 0.8448 (0.0006) | — | 0.00 | −0.1267 (0.0346) | 62.78% (0.78) | 0.011862 | 0.007745 | 기준선 |
| D1 | + 거시 원본만 | 245 | 0.8450 (0.0022) | +0.0003 | 1.52 | −0.4549 (0.0604) | 76.17% (1.15) | 0.011833 | 0.009393 | **실패** |
| D3 | + 거시 원본 + 상호작용 8종 | 253 | 0.8388 (0.0005) | **−0.0059** | 2.21 | −0.4526 (0.0103) | 89.13% (0.29) | 0.011768 | 0.010973 | **실패** |
| D6 | + 선별 상호작용 3종 (제약 없음) | 159 | 0.8443 (0.0006) | −0.0004 | 0.53 | −0.1010 (0.0331) | 63.95% (0.52) | 0.011897 | 0.007885 | **부분성공** |
| D6m | D6 + 단조 제약 | 159 | 0.8441 (0.0004) | −0.0006 | 0.45 | −0.0697 (0.0347) | 65.88% (0.62) | 0.011830 | 0.008118 | **실패** |

### ★ 단조 제약 적용 전후 (지시 사항)

| | D6 (제약 없음) | D6m (제약) | 차이 |
|---|---|---|---|
| Valid AUC | 0.8443 (0.0006) | 0.8441 (0.0004) | **−0.0002** |
| PD-부도율 상관 | −0.1010 | −0.0697 | +0.0313 (개선 방향) |
| 건수 MAPE | 63.95% | 65.88% | +1.93%p (악화 방향) |
| Brier | 0.011897 | 0.011830 | −0.000067 (개선) |
| ECE | 0.007885 | 0.008118 | +0.000233 (악화) |

- **제약은 AUC 를 깎지 않는다.** −0.0002 는 노이즈 규칙(0.003)의 15분의 1 이고
  시드 표준편차(0.0004~0.0006)보다도 작다. 판별력 비용은 **없다**고 봐도 된다.
- 대신 확률 **수준(level)** 을 약간 눌러 건수 MAPE 가 1.93%p 늘었다.
  이 폭이 D0 대비 2σ(1.98%p) 선을 넘겨 **D6m 만 '실패' 로 떨어진다.**
  같은 제약이 Brier 는 개선하고 ECE 는 악화시킨다 — 방향이 갈리므로
  "제약이 캘리브레이션을 망친다" 고 단정할 수 없다. **MAPE 한 지표에 걸린 판정**이다.
- 상관은 제약 쪽이 더 낫다(−0.1010 → −0.0697). 다만 2σ(0.098) 이내라 '개선' 으로
  치지 않는다.
- 판정 규칙이 MAPE 악화를 즉시 '실패' 로 보내므로, **제약 채택 여부는 규칙이 아니라
  용도로 갈라야 한다.** 외삽(스트레스 시나리오)에서 부호 안정성이 필요하면 제약을,
  건수 추정이 목적이면 무제약을 쓴다. 어느 쪽도 D0 를 이기지 못한다.

### D6 3종의 기여 (시드 42)

| 항 | 정의 | 단조 | gain% (D6) | 순위 | gain% (D6m) |
|---|---|---:|---:|---:|---:|
| `credit_spread_lv_x_lev` | `LV_credit_spread` × `exp_rate` | +1 | 0.488 | 45 | 0.476 |
| `bsi_x_industry` | `BSI_mfg_biz_yoy` × `is_manufacturing` | −1 | 0.114 | 60 | 0.081 |
| `fx_shock_x_export` | `USD_KRW_log_ret` × `exp_fx` | 0 | **0.000** | **159 (최하위)** | 0.000 |

- `fx_shock_x_export` 는 **한 번도 분기에 쓰이지 않았다.** D축 초판에서도 gain 0.000 /
  255위였고, 정정된 거시 데이터에서도 같다. 충격 정의(월간 로그수익률)인지 노출도
  (`exp_fx`, 결측 89.51%)인지는 이 실행으로 갈리지 않는다.
  `export_price_index_KOR × exp_fx` 와의 대조는 E2 후보로 남는다.
- `credit_spread` **수준**을 쓴 항이 세 항 중 가장 크다. 정정으로 신용스프레드가
  제 계열이 된 뒤의 결과다 (음수 개월 68/77 → 0/77).

### CPI_core 를 D6 에서 제외한 근거 (기록)

- 누적 지수의 단조 증가와 부도율의 단조 증가가 겹친 **공통 추세**이며 인과가 아니다.
  단조 제약을 걸면 외삽 시 발산한다.
- 실측 뒷받침: E0 재검증에서 `CPI_core` 수준의 Train/Valid 상관이 정정 후
  **+0.978 / +0.910** 으로 오히려 강해졌다. 두 계열이 같은 방향으로 자라기만 하는
  구간에서는 상관이 커지는 것이 당연하고, 그것이 인과의 근거가 되지는 않는다.
- 이번 D축 §5-1 에서도 `CPI_core_yoy` 는 Train +0.834 / Valid −0.132 로 **부호가
  뒤집힌다.** 제약을 걸 대상이 아니다.

### 유지되는 결론 / 달라진 것

- **유지**: 거시 원본과 상호작용 8종을 넣으면 캘리브레이션이 악화된다.
  정정된 데이터로도 D1·D3 은 실패다. 원인도 그대로다 — 금리 계열의 Train/Valid
  부호 반전(`base_rate_diff12` +0.904 / −0.909)과 부도의 후행성.
- **달라진 것**: `credit_spread_diff12` 는 이제 **부호가 유지된다**
  (Train +0.462 / Valid +0.861). 정정 전에는 뒤집혀 있었다 —
  기간 스프레드를 신용 스프레드라고 부르던 탓이다.
- **새로 알게 된 것**: 상호작용을 8종에서 **선별 3종으로 줄이면 '악화' 가 사라진다**
  (D3 실패 → D6 부분성공). 악화의 원인이 '거시를 넣는 것' 자체가 아니라
  **부호가 불안정한 항을 함께 넣는 것**이라는 뜻이다. 다만 D6 도 **개선은 아니다.**

### 남은 오염 — D축 전 시나리오 공통

`exp_fx_dbt` 는 D축 패널에서 여전히 **(d) 부분오염**이다 (위 5번 참조).
D0 의 기업 고유 피처에 들어 있으므로 전 시나리오가 이 오염을 공유한다.
D6 의 3개 항은 AC12 를 쓰지 않으므로 **D6 판정 자체는 이 오염과 무관**하다.
`fx_vol_x_fxdebt`(D1·D3 에 포함)는 직접 영향을 받는다. 조치는 12번 항목.

### 산출물

- 결과: `d_axis/d_axis_results.json` · `d_axis/d_axis_summary.json`
- 리포트: `D_AXIS_RESULT.md` (재생성). **구 세대는 `D_AXIS_RESULT_preGroupD.md` 로 보존.**
  구 파일의 2026-09-01 정정 주석은 그 세대 수치에 대한 것이므로 함께 보존된다.
- 구 결과 원자료: `d_axis/d_axis_results_preGroupD.json` · `_summary_preGroupD.json`
- 구 패널: `nh_panel_macro_12m_obv_none_real_preGroupD.parquet`

### 리포트 생성기 결함 1건 수정 (step35)

`step35_d_axis_report.py` 가 서술 문장에 **수치를 하드코딩**하고 있었다
(`+0.904` / `−0.909`, `D0 −0.127 → D3 −0.457`, `MAPE 62.78% → 87.84%`,
`202401 51.7건 → 202505 10.0건`, `k=24 에서 Train +0.734`, `BSI Train −0.757` 등).
재실행하면 표는 갱신되는데 **서술이 옛 수치를 계속 주장하는** 구조였다.
전부 데이터에서 계산해 쓰도록 바꿨고, 서술의 방향 판단(뒤집힘/증가/감소)도
관측값 부호로 결정하게 했다. 실행일·시드·시나리오 목록·기저율도 파생값이다.

전수 스캔에서 3건이 더 나왔다 (2026-09-02 추가 조치).

| 위치 | 문제 | 조치 |
|---|---|---|
| §3 주석 | `53.14%` 가 **두 번째로** 하드코딩돼 있었다 (§0 만 고쳤었다) | 게이트 1 기록에서 읽는다 |
| §3 주석 | "금리 경로는 `credit_spread_x_lev` 와 `liq_spread_x_shortdebt` 가 커버한다" — 2026-09-01 에 **무효 판정을 받은 논거**가 생성기에 그대로 남아 있었다 | 단정을 없애고 두 항의 gain·순위·충격 0 인 달을 수치로 적는다 (현재: 0.142/58위, 0.011/102위) |
| §4 연도별 표 | `ACTUAL_YEARLY` 상수가 **구 세대 패널의 값**이었다. 현재 패널 실측과 매년 0.003~0.020%p 어긋났다 (전부 상수가 높은 쪽) | `yearly_default_rates()` 로 패널에서 실측. 상수는 `ACTUAL_YEARLY_FALLBACK` 으로 내리고, 폴백을 쓰면 리포트에 그 사실을 밝힌다 |

또 `Valid 312,320행` 은 반올림된 백분율(53.14%)에서 **역산한 값**이었다.
실제는 312,334 행이다. 없는 정밀도를 만드는 계산이므로 원자료의 `n_valid` 를
그대로 읽게 바꿨다. 판정 규칙 표의 `0.003` 도 `NOISE_BAND` 를 참조한다.

> 남은 하드코딩 데이터 값은 **없다.** `REFERENCE_MACRO_GAIN_PCT`(5.12, 원본 모델의
> 거시 gain 합계)와 `NOISE_BAND`(0.003)는 상수가 맞다 — 전자는 비교 기준이 되는
> 과거 모델의 성질이고 후자는 기준서가 정한 임계다.

---

## 8. CRIF 누수 재측정 — 한 세대 안에서 (2026-09-02)

6번의 `A2 +0.0679` / `B4f +0.0018` 은 세대가 다른 두 측정을 나란히 놓은 것이었다.
**AC12 결함(4번)을 고친 뒤 같은 패널·같은 규제(R2)·같은 시드 3회로 전부 다시 쟀다.**

| 측정 | CRIF 조인 | 피처 | Valid AUC (3시드 평균) | A0 대비 ΔAUC |
|---|---|---:|---|---:|
| `A0` | 없음 (기준선) | 152 | 0.84369 (σ 0.00045) | — |
| `A2` | **(V_BZNO, 연도)** — `CRDBD_OCU_YY` 를 `str[:4]` 절단 | 156 | **0.88160** (σ 0.00289) | **+0.03791** |
| `B46` | 월 단위 as-of (AC12·CRIF 교정 패널, A0 피처만) | 152 | 0.84436 (σ 0.00081) | +0.00067 |
| `B4f` | **월 단위 as-of** + CRIF 3종을 피처로 | 155 | **0.84353** (σ 0.00094) | **−0.00016** |

시드별 값 — A0 0.84351 / 0.84337 / 0.84420 · A2 0.88492 / 0.87964 / 0.88024 ·
B46 0.84352 / 0.84440 / 0.84514 · B4f 0.84360 / 0.84255 / 0.84444
(원자료: `stage6_ablation/seed_variance_probe.json` · `stage6_ablation/ablation_B_results.json`)

- **연 단위 조인의 기여 +0.0379 → 시점 정합화 후 −0.0008 (B46 대비).**
  A2 의 ΔAUC 는 시드 표준편차(0.0029)의 13배로, 노이즈로 설명되지 않는다.
  B4f 의 −0.0008 은 표준편차(0.0009) 수준이다.
  노이즈 규칙 `|ΔAUC| < 0.003` 이내이고 **부호도 음수**다.
  원본 모델에서 gain 36.34%(1위)였던 기여가 시점을 맞추자 **완전히 사라진다.**
  → **그 기여는 전부 누수였다.** `LEAK_CONFIRMED` 유지 판정의 직접 근거다.
- **`AC12` 정합화의 실제 효과 = +0.00067** (A0 → B46, 노이즈 이내).
  결함을 고친 뒤 처음으로 제대로 잰 값이다. "성능 동일" 이라는 기존 서술은
  **결론은 맞았지만 근거가 없었다** — 이제 근거가 있다.
  성능이 같아도 `exp_fx_dbt` 의 (d) 부분오염이 해소되므로 정합화를 유지한다.

> **대조의 한계 (명시)**: `A2` 는 집계 4종(`CRIF_EVENT_CNT`, `CRIF_RSN_AM_SUM`,
> `CRIF_OVD_AM_SUM`, `CRIF_WORST_RSNC`), `B4f` 는 as-of 3종(`CRIF_CNT_12M`,
> `CRIF_MONTHS_SINCE`, `CRIF_WORST_RSNC`)이다. 금액 합계를 as-of 쪽에 넣지 않았으므로
> **피처 집합이 완전히 같지는 않다.** 대조가 성립하는 층위는 "같은 원천을 어느 시점
> 단위로 붙였는가" 이고, 개별 피처 1:1 대응은 아니다. 금액 합계를 뺀 이유는
> 규모 효과가 섞여 해석이 흐려지기 때문이다 (6번 참조).

### ★ 신규 발견 2 — `--seed-probe` 가 되넣기 컬럼을 조인하지 않았다

첫 A2 재측정이 **A0 와 완전히 동일한 AUC**(0.8434 / 0.8435 / 0.8442)를 냈다.
피처 수도 152 로 A0 와 같았다. 시드까지 일치하는 완전 동일값이라 우연이 아니다.

- 원인: `step30` 이 `need_pre5`(원천에서 되넣을 컬럼)를 **`targets` 에서만** 계산했고,
  `targets` 는 `--run` / `--run-all` 로만 채워졌다. `--seed-probe A2` 는 `targets` 를
  비운 채 돌아가 CRIF 4개를 조인하지 않았고, `resolve_features` 의
  `if a in available` 이 그 컬럼을 **조용히 건너뛰었다.**
- 결과: "A0 + 누수 4개" 를 재려던 시나리오가 A0 그 자체로 돌아가 ΔAUC 0 을 냈다.
  이 값을 그대로 믿으면 **"누수 기여가 없다"** 는 정반대 결론이 나온다.
- 조치 2건:
  1. `--seed-probe` / `--reg-probe` 시나리오를 `targets` 에 합친다.
  2. `resolve_features` 에 `strict=True` 를 넣어 **되넣기 컬럼이 없으면 예외로 중단**한다.
     측정하려던 것을 측정하지 못했으면 결과가 아니라 오류다.
- 수정 후 A2 는 피처 **156** 개로 돌아가고 AUC 0.8816 이 나왔다.

> **같은 계열의 결함이 이번 작업에서 3건 나왔다** — AC12 리네임 누락(4번),
> G1-7 프로브 기대값 고정(7번), 그리고 이 건. 모두 **"조용히 아무 일도 하지 않고
> 통과하는"** 형태다. 세 곳 전부 **실패하면 멈추는 가드**로 바꿨다.
> 남은 위험은 같은 형태가 아직 발견되지 않은 곳에 있다.

| # | 필요 조치 | 상태 |
|---|---|---|
| 14 | `ablation_A_results*.json` 의 구세대 A0~A9 수치 재측정 여부 판단 | **대기** — A2 만 재측정했다. A1/A3/A4/A7 도 `--seed-probe` 로 측정한 값이 있으면 같은 결함의 영향을 받는다. `--run` 으로 측정한 값은 무관 |

---

# [2026-09-02] 14번 A축 재측정 여부 · 11번 경계선 15건 · 2번 하드코딩 전수

## 9. [14번] A축 재측정 — **불필요.** 확정판은 조인 누락의 영향을 받지 않았다

저장된 A/B/C축 결과 **47건 전수**를 피처 수로 대조했다
(스크립트: 세션 스크래치패드 `audit_feature_counts.py`).
판정 기준은 조인 누락의 전형적 신호 — **`실제 피처 수 == A0 피처 수` 인데 `add > 0`**.

### 결론 1 — 조인 누락 신호는 **0건**

확정판 `step30_ablation_A_results.md` 의 15개 시나리오 피처 수가
**현재 시나리오 정의와 정확히 일치**한다.

| ID | 저장 | 현재 정의 기대 | ID | 저장 | 기대 |
|---|---:|---:|---|---:|---:|
| A0 | 152 | 152 (기준) | A7 | 162 | 152+10 = 162 |
| A1 | 153 | 152+1 | A8 | 116 | 가변(sentinel) |
| A2 | 156 | 152+4 | A0c | 150 | 152−2 |
| A3 | 154 | 152+2 | C0 | 150 | 152−2 |
| A4 | 153 | 152+1 | C1 | 151 | 152−1 |
| A5 | 151 | 152−1 | C2 | 151 | 152−1 |
| A6 | 151 | 152−1 | C4 | 150 | 152−2 |
| | | | C5 | 114 | 가변(sentinel) |

### 결론 2 — 오늘 재측정한 A0 · A2 가 확정판을 **비트 단위로 재현**한다

| | 피처 | best_iter | Train | Dev | **Valid** | K-S | PSI |
|---|---:|---:|---:|---:|---:|---:|---:|
| A0 확정판(md) | 152 | 4,813 | 0.9982 | 0.9700 | **0.8434** | 0.5307 | 0.0167 |
| A0 오늘(seed 42) | 152 | 4,813 | 0.9982 | 0.9700 | **0.8434** | 0.5307 | 0.0167 |
| A2 확정판(md) | 156 | 4,916 | 0.9986 | 0.9695 | **0.8796** | 0.6078 | 0.0167 |
| A2 오늘(seed 42) | 156 | 4,916 | 0.9986 | 0.9695 | **0.8796** | 0.6078 | 0.0167 |

`best_iteration` 까지 완전히 같다. 확정판 A축 표는 **재측정 불필요**다.
→ 사용자가 제시한 규칙 "불일치가 있으면 해당 시나리오만 재측정" 에 따라 **재측정 없음.**

### ★ 대신 나온 문제 2건

#### (a) 구세대 파일 2개는 시나리오 정의가 다르고, A5·A6 는 **부호가 반대**다

| 파일 | A0 | 성격 |
|---|---:|---|
| `ablation_A_results.json` (08-30 19:16) | 150 | 구세대. A0 = "누수·의심·CG01/C302 전부 제외" |
| `ablation_A_results_R2_v1_preC302.json` (08-30 20:03) | 150 | R2, **C302 정합분 편입 이전** |
| `step30_ablation_A_results.md` (08-30 21:51) | 152 | **확정판.** A0 = "시점 정합 피처 전부 (C302 정합분 편입)" |

| ID | 당시 정의 (구세대 파일) | 현재 정의 | 방향 |
|---|---|---|---|
| A5 | A0 **+** C302_CRI_ORD + C302 계열 전체 | A0 **−** C302_CRI_ORD | **반대** |
| A6 | A0 **+** C302 이력 유무 플래그만 | A0 **−** C302_MISSING_YN | **반대** |
| A3 | A0 + CG01_KIS_SCORE + CG01_MISSING_YN | A0 + CG01 점수 + 이력플래그 | 같음(표현만) |
| A7 | 복원분 7 + LEAK_SUSPECT + CG01/C302 전체 (14개) | 누수 변수 전부 (10개) | 범위 축소 |
| A0 | 누수·의심·CG01/C302 전부 제외 (150) | 시점 정합 피처 전부 (152) | **기준선 이동** |

> **위험**: 구세대 JSON 을 **현재 코드의 시나리오 설명으로 읽으면 A5·A6 는 정반대 해석이
> 된다.** 저장된 A5 0.8433 은 "A0 에 C302 계열을 **넣었을 때**" 이고, 현재 정의로 읽으면
> "A0 에서 CRI_ORD 를 **뺐을 때**" 가 된다. 조인 누락과 같은 급의 오독 위험이다.
> 각 레코드에 `desc` 가 남아 있어 판별은 가능하다 — **반드시 `desc` 를 함께 읽을 것.**

사용자가 인용한 수치의 출처를 정리하면 아래와 같다.

| 인용 | 출처 | 그 파일의 A0 | 확정판 대응값 |
|---|---|---:|---|
| A7 0.9412 | `_R2_v1_preC302` (구세대) | 0.7826 | **A7 0.9398 / A0 0.8434 → ΔAUC +0.0965** |
| A3 0.8993 | `_R2_v1_preC302` (구세대) | 0.7826 | **A3 0.9217 / ΔAUC +0.0784** |
| C1 0.8570 | 확정판 md (시드 3회 평균) | 0.8434 | **유효 — 그대로 사용 가능** |
| 제안 58 (CG01 43.7% / C302 85.8%) | 확정판 md §5 에 **"R0·구 기준선 값"** 이라고 이미 명시돼 있다 | — | 재분해 필요 시 별건 |

→ **STAGE 7 보고서에는 확정판 md 의 값을 쓸 것.** 구세대 두 파일은 아카이브다.

#### (b) ★ 확정판 A축 표의 **원자료 JSON 이 유실됐다**

확정판 md 는 원자료를 `ablation_A_results_R2.json` 이라고 적었으나 그 파일에는
**C4 · C5 2건만** 남아 있다 (08-30 21:06). `step30._merge_results` 의 docstring 이
그 사고를 기록하고 있다 — *"예전 구현은 파일 전체를 [C4, C5] 로 교체해 앞서 돌린
14종을 지워 버렸다 (실제로 한 번 발생했다)"*. 병합 로직은 **사고 이후** 추가됐고,
데이터는 이미 사라진 상태였다. **md 의 표가 유일한 기록이다.**

| # | 필요 조치 | 상태 |
|---|---|---|
| 14 | A축 확정판 재측정 | **불필요 — 종결.** 피처 수 전수 일치 + A0·A2 비트 단위 재현 |
| 15 | 확정판 원자료 JSON 복원 (`--run-all` 1시드, 16종 ≈ 25분) | **판단 대기.** md 표는 있으나 gain 목록·캘리브레이션 원자료가 없다. STAGE 7 에서 필요하면 복원 |
| 16 | 구세대 2개 파일에 "아카이브 · A5/A6 정의 반대" 헤더 명시 | **대기** |

## 10. [11번] 경계선 15건 — 표와 D축 영향 판단

### 15건 실측 (측정일 2026-09-01, 2026-09-02 재측정 동일)

| series_name | 현재 그룹 | shift | 실데이터 최종월 | 경과 | 경과−시차 |
|---|---|---:|---|---:|---:|
| `CPI_core` | B | 1 | 2026-07 | 2 | **+1** |
| `CPI_core_excl_food_energy` | B | 1 | 2026-07 | 2 | **+1** |
| `CPI_food_nonalcohol` | B | 1 | 2026-07 | 2 | **+1** |
| `PPI_total` | B | 1 | 2026-07 | 2 | **+1** |
| `export_price_index_KOR` | B | 1 | 2026-07 | 2 | **+1** |
| `export_index` | B | 1 | 2026-07 | 2 | **+1** |
| `import_index` | B | 1 | 2026-07 | 2 | **+1** |
| `M1_narrow_money` | C | 2 | 2026-06 | 3 | **+1** |
| `M2_broad_money` | C | 2 | 2026-06 | 3 | **+1** |
| `Lf_liquidity` | C | 2 | 2026-06 | 3 | **+1** |
| `monetary_base_sa` | C | 2 | 2026-06 | 3 | **+1** |
| `current_account` | C | 2 | 2026-06 | 3 | **+1** |
| `goods_balance` | C | 2 | 2026-06 | 3 | **+1** |
| `unsold_housing` | C | 2 | 2026-06 | 3 | **+1** |
| `construction_cost_index` | C | 2 | 2026-06 | 3 | **+1** |

- 15건 **전부 `경과 = 시차 + 1`** 이다. `경과 == 시차` 인 지표는 **0건**.
- 지시 규칙 *"경과월 > 시차 인 지표만 이동 대상, 같으면 유지"* 를 문자 그대로 적용하면
  **15건 전부가 이동 대상**이 된다 (B→C 7건, C→D 8건).
- ★ 그러나 지시서 표제는 *"실제 공표일 관측 후 재판정"* 이다. 두 지시가 갈린다.
  **측정일이 월초(9/1, 9/2)** 라 월간 통계의 익월 중순 공표를 아직 못 본 상태일 수 있고,
  `shift(+1)` 이 묻는 것은 "8월 값을 **9월 안에** 쓸 수 있는가" 다. 9/1·9/2 결과가
  같았으므로 하루 차이로 갈리는 문제도 아니다. **이동 여부는 승인 대기로 둔다.**

### ★ D축 재실행 필요 여부 — **불필요**

15건에서 파생된 컬럼(`_yoy`, `_yoy_ma3m`, `_diff12`, `_diff12_ma3m`)이 D축 gain
상위에 들어가는지 실측했다.

| 시나리오 | 전체 gain 상위 20 안 | 거시 gain 상위 10 안 | 최대 gain / 최고 순위 |
|---|---:|---:|---|
| D0 | 0개 | 0개 | 거시 없음 |
| D1 | **0개** | 6개 | `import_index_yoy` 0.132% / 59위 |
| D3 | **0개** | 4개 | `import_index_yoy` 0.152% / 59위 |
| D6 · D6m | **0개** | 0개 | 3종에 포함되지 않음 |

- **전체 gain 상위 20 에 단 하나도 없다.** 거시 gain 상위 10 안에는 들어가지만
  그 자체가 전체 55~76위이고 개별 gain 은 최대 0.152% 다.
- D6/D6m 은 선별 3종만 쓰므로 15건과 **무관**하다. D6 판정은 영향받지 않는다.
- → **시차를 옮기더라도 D축은 재실행하지 않는다.** `impute_data` 재실행 후
  `step6` 재생성만 하고, D축 결과는 유지한다. 단 이동이 확정되면
  **D1·D3 의 거시 gain 구성(위 표의 6개·4개)이 바뀌므로 그 표만 갱신**한다.

> 이동을 승인하면 실행 범위: `impute_data` → `step6 --tag real` → `step33`(게이트 1)
> → D축은 **유지**. 약 5분. D축 재실행(15회 학습 ≈ 40분)은 필요 없다.

### 측정일 의존성과 자동 기록 — 이미 반영됨

- `data_collector.check_publication_lag` 가 수집마다 `수집일 − 실데이터 최종월` 을
  재고 그룹 배정과 어긋나면 경고한다. 임계는 **`lag > shift`** 로 조였다.
- 기록 위치: `output/metadata/collected_series_meta.json` 의 `publication_lag_months`.
- 별도 전수 재검사 스크립트: `api_data_processing/audit_publication_lag.py`
  → 리포트 `PUBLICATION_LAG_AUDIT.md`, 원자료 `output/metadata/publication_lag_audit.csv`.
- 측정일 의존성 경고는 `data_preprocessing.md` Phase 1 절에 명시했다.

## 11. [12번] AC12 오염 — 각주 반영 + 추가 확인 결과

### D_AXIS_RESULT.md 각주 (생성형)

`step35.ac12_contamination_note()` 를 추가했다. 수치를 하드코딩하지 않고 패널 컬럼과
gain 원자료에서 읽는다. 재생성해도 각주가 유지되고 수치가 갱신된다.

### 오염 범위 — 예상보다 넓다 (정정)

직전 보고에서 "D0 의 `exp_fx_dbt` 가 (d)" 라고만 적었으나, 실측하니
**`AC12_*` 금액 10개와 `HAS_AC12_YN` 자체가 D축 base 피처에 들어 있다.**

| 시나리오 | AC12 관련 포함 피처 |
|---|---|
| D0 · D1 · D6 · D6m | `AC12_*` 10개 + `HAS_AC12_YN` ((b)) + `exp_fx_dbt` ((d)) |
| D3 | 위 + `fx_vol_x_fxdebt` ((d)) |

### 영향 크기 — 제한적

- `AC12_*` 금액 10개 · `HAS_AC12_YN` · `exp_fx_dbt` 는 **어느 시나리오에서도
  전체 gain 상위 20 밖**이다 (D0 상위 20 은 `C302_CRI_ORD` 27.2% / `STD_INDS_MID2`
  19.0% / `JEMU_*` 로 채워진다).
- `fx_vol_x_fxdebt` (D3 전용): gain **0.000 / 0.023 / 0.004%**, 순위 **177 / 89 / 129위**
  (시드 7 / 42 / 2024). 판정을 뒤집을 크기가 아니다.
- 전 시나리오가 같은 오염을 공유하므로 **ΔAUC 비교는 공정**하다. 절대값만 오염을 포함한다.

### ★ 추가 확인 — `HAS_AC12_YN` 은 살아 있다

`exp_fx_dbt` 결측을 0 으로 채운 것이 "외화부채 없음" 과 "모름" 을 뭉개지 않는지
확인하라는 지시에 대한 답: **`HAS_AC12_YN` 이 D0~D6m 전 시나리오의 피처에 포함돼
있다.** 모델은 두 경우를 구분할 수 있다. **추가 플래그는 필요하지 않다.**

- B46 as-of 재구성에서도 `HAS_AC12_YN` 을 "이월 상한 내 유효 레코드 존재" 로 다시
  세우고, `AC12_STALE_MONTHS`(정보가 얼마나 묵었는가)를 추가로 만든다.
  단 `AC12_STALE_MONTHS` 는 D축 패널(step6 계열)에는 **없다** — B46 전용이다.
  본류 반영(12번) 시 함께 들어온다.

### 07_한계와_향후과제 기재 문안 (지시대로)

> `exp_fx_dbt` 및 `fx_vol_x_fxdebt` 는 상류 `AC12_TOTAL_KRW_AM` 이 연 단위 조인분이라
> 시점 부분오염((d))을 포함한다. AC12 를 월 단위 as-of 로 재조인하면 해소됨을
> 확인했으나(B46 — step31 재판정에서 (b) 11개·(d) 2개가 모두 해소, 성능 기여는
> A0 대비 +0.00067 로 노이즈), 본류(step2/step5) 반영 시 A/B/C/D 전 축의 기준선이
> 함께 이동하므로 이번 범위에서는 적용하지 않았다. 영향 크기는 제한적이다 —
> 해당 변수들은 전 시나리오에서 gain 상위 20 밖이며 `fx_vol_x_fxdebt` 는
> D3 에서 gain 0.000~0.023%(89~177위)다.

## 12. [2번] 하드코딩 기대값 전수 확인 — step30~step35

G1-7 과 같은 "설정 변경을 반영하지 못하는 고정 기대값" 패턴을 전 파일에서 찾았다.

| 파일 | 발견 | 판정 / 조치 |
|---|---|---|
| `step31` | `expect = f"{config.PUB_LAG_MONTHS:02d}"` (JEMU 변경월 기대) | **정상** — 이미 config 파생 |
| `step33` | G1-1 의 `"202101"`, G1-4 의 `ref = pm.loc["202201"]` | **판정 기준 자체**다. G1-1 은 "거시 원천 첫 행 ≤ 2021-01" 이 기준서 문구이고 G1-4 는 "2021년 값 ≠ 2022-01 값" 이 검사 설계다. 유지 |
| `step33` | G1-7 프로브 기대 시차 | **수정 완료** — `impute_data.GROUP_*_COLS` 파생 |
| `step30` | `("202307", "Dev 6개월")` | **정상** — 대체 Dev 창을 재는 **실험 파라미터**다. 기대값이 아니다 |
| `step34` | 분할 경계 | **정상** — `split_spec.DEV_START` / `VALID_START` 참조 |
| **`step35`** | **분할 경계를 6곳에 하드코딩** (`"202310"` ×2, `"202401"` ×2, 구간 표의 `("202101","202309")`·`("202401","202505")`) | **★ 수정 완료** — `split_spec` 파생 + 관측 최소·최대월 파생 |
| `step35` | `ACTUAL_YEARLY` 상수 | **수정 완료** (위 7번 참조) — 패널 실측으로 대체 |
| `step32` / `step32b` | 해당 없음 | — |

### `step35` 분할 경계가 위험했던 이유

§5 의 Train/Valid 상관 진단이 **모델이 쓴 분할과 다른 경계**로 계산될 수 있었다.
`split_spec` 을 바꾸면 모델은 새 분할로 학습하는데 이 진단만 옛 경계를 쓰고,
리포트는 둘을 같은 분할이라고 제시한다. 부호 반전 분석 전체의 근거가 흔들린다.
현재 값은 우연히 일치했으므로 **수치는 바뀌지 않았다.** 잠재 위험만 제거했다.

| # | 필요 조치 | 상태 |
|---|---|---|
| 17 | `step19` / `step23b` 등 다른 리포트 생성기에도 같은 패턴이 있는지 확인 | **대기** — step30~35 범위 밖이라 이번에 보지 않았다 |

---

# [2026-09-02] 결정 반영 — 시차 유지 · 가드 보정 · A축 값 통일 · 문서 뼈대

## 13. [11번 결정] 15건 **전부 유지.** 이동하지 않는다

승인된 근거를 그대로 옮긴다.

- `경과 − 시차` 가 15건 전부 **일률적으로 +1** 이다. Group B(+1) 7건과 Group C(+2)
  8건은 **시차가 다른 두 그룹**인데 편차가 같다. 개별 지표의 공표 지연 문제라면
  값이 흩어져야 한다. 균일한 +1 은 지표 특성이 아니라 **측정 방법의 공통 편향**이다.
- 원인은 측정일이 월초(2026-09-01)라는 것이다.

  | | 공표 | 9/1 시점 | 판정 |
  |---|---|---|---|
  | CPI 7월분 | 8월 초 | 가용 | 시차 +1 정합 |
  | CPI 8월분 | 9월 초 | 미공표 | — |

  경과 = 2026-09 − 2026-07 = 2 로 계산되지만 **9월분은 애초에 존재할 수 없는 달**이다.
  월초 측정으로 한 달치를 덜 본 착시다. 월말에 측정했다면 `경과 − 시차 = 0` 이었다.
- 표제("실제 공표일 관측 후 재판정")와 규칙("경과 > 시차만 이동")이 갈렸고,
  **표제 쪽 취지를 따라 유지로 확정**했다. 규칙을 문자 그대로 적용하면 측정일 편향이
  시차 판정을 오염시킨다.

### 가드 임계 조정 — (a) 월초 보정 채택

`lag > shift` 는 월초 측정에서 전 지표를 걸리게 해 과도하게 엄격했다.
**원인에 직접 대응하는 (a)** 를 택했다 — 측정일이 그 달 15일 이전이면 경과에서 1을
차감한다 (`MONTH_START_CUTOFF_DAY = 15`). 임계는 `lag > shift` 를 **유지**한다.
보정 후에도 걸리는 지표는 **진짜 look-ahead 후보**다.

> ★ **일률 보정은 자기 발등을 찍는다 — 주기별로 적용했다.**
>
> 보정을 전 지표에 똑같이 적용하면 분기 3건(`household_credit` · `household_loan` ·
> `current_account_quarterly`)이 경과 3 → 2 가 되어 **Group C(+2) 로도 통과**한다.
> 그러면 **가드가 Group D 이동 사유를 스스로 못 잡게 된다.**
>
> 보정의 근거는 "직전월 공표를 아직 반영하지 못했다" 이고, 그런 직전월 공표가
> 존재하는 것은 **월간 계열뿐**이다. 분기 계열에는 놓칠 직전월분이 없다.
> 그래서 **관측 주기를 데이터에서 실측**해(`_observed_month_step` — 수록월 간격의
> 중앙값) 월간(간격 1)일 때만 보정한다. 설정의 `frequency` 를 믿지 않는다 —
> 매핑 사고가 두 번 있었던 프로젝트에서 설정값은 정본이 아니다.

### 조정 후 전수 재검사 — **걸림 0건**

| 그룹 | 대상 | `경과 − 시차` 분포 | 걸림 |
|---|---:|---|---:|
| A (+0) | 35 | 0 × 35 | 0 |
| B (+1) | 10 | −1 × 3, 0 × 7 | **0** |
| C (+2) | 17 | −2 × 7, −1 × 1, 0 × 8 | **0** |
| D (+3) | 3 | 0 × 3 (주기 3개월 인식 → 보정 미적용) | **0** |

- 15건이 전부 월초 착시였음이 확인됐다. **진짜 look-ahead 후보는 없다.**
- Group D 3건은 경과 3 = 시차 3 으로 통과하고, **Group C(+2) 였다면 여전히 경고**한다
  (직접 검증). Group D 이동 사유가 가드로도 재현된다.
- ★ 감사 스크립트 결함 1건 동반 수정: `audit_publication_lag` 이
  `check_publication_lag` 에 **최종일 하나만** 넘겨 관측 주기를 추정할 수 없었다.
  전체 수록일을 넘기도록 고쳤다. 고치기 전에는 분기 3건이 경과 2 로 잘못 나왔다.

### D축 재실행 — 불필요 (승인)

15건 파생이 전체 gain 상위 20 에 **0개**, D6/D6m 거시 상위 10 에도 **0개**.
시차를 옮기지 않으므로 애초에 재생성도 불필요하다.

## 14. [14번 정정] A축 수치를 확정판으로 통일

앞으로 모든 문서·리포트에서 **구세대 값을 쓰지 않는다.**

| 항목 | 구세대 (쓰지 말 것) | **확정판 (정본)** |
|---|---|---|
| A0 피처 수 | 150 | **152** (C302 정합분 편입 후) |
| A7 (누수 상한) | 0.9412 | **0.9398 / ΔAUC +0.0965** |
| A3 (CG01 최대 기여) | 0.8993 | **0.9217 / ΔAUC +0.0784** |
| C1 (최종 구성) | — | **0.8570** (시드 3회 평균, σ=0.0025) — 확정판 그대로 유효 |

- **C1 = 0.8570 이 확정판 값으로 유효하다.** 최종 구성 선택 근거는 흔들리지 않는다.
- A5·A6 정의 부호 반전은 **현재 정의가 맞다** — C302 가 A0 에 편입되면서 "추가"
  시나리오가 "제거" 시나리오로 전환된 것이다. 문서에는 현재 정의
  (`A5 = A0 − C302_CRI_ORD`, `A6 = A0 − C302_MISSING_YN`)로 쓰고,
  전환 사실을 각주로 남긴다 (`docs/03_모델링과_검증.md` 에 기재 완료).

### 제안 58 — 산출식 검증과 확정판 재산출

먼저 **산출식을 역산으로 확인**했다. 구세대(`_R2_v1_preC302`, A0=0.7826) 값을 넣으면
인용값이 정확히 재현된다.

| 비율 | 산출식 | 구세대 계산 | 인용값 |
|---|---|---|---|
| CG01 | `(A4−A0)/(A3−A0)` | (0.8336−0.7826)/(0.8993−0.7826) = **43.7%** | 43.7% ✓ |
| C302 | `(A6−A0)/(A5−A0)` | (0.8347−0.7826)/(0.8433−0.7826) = **85.8%** | 85.8% ✓ |

당시 A5·A6 는 ADD 시나리오였으므로 위 식이 성립한다. 확정판에서는 C302 가 A0 에
들어가 있으므로 **"둘 다 없음" 기준선이 `A0c`** 로 바뀐다.

**확정판(R2, A0=152) 재산출 — 잠정** (md 표 기준. 복원 JSON 으로 재확인 중)

| 분해 | 계산 | 몫 |
|---|---|---:|
| CG01 총효과 | `A3 − A0` = 0.9217 − 0.8434 | +0.0783 |
| CG01 **이력 유무 단독** | `A4 − A0` = 0.8863 − 0.8434 = +0.0429 | **54.8%** |
| C302 총효과 | `A0 − A0c` = 0.8434 − 0.7826 | +0.0608 |
| C302 **이력 유무 단독** | `A5 − A0c` = 0.8336 − 0.7826 = +0.0510 | **83.9%** |
| C302 **등급 서열 단독** | `A6 − A0c` = 0.8465 − 0.7826 = +0.0639 | **105.1%** |

- 구값 대비: CG01 43.7% → **54.8%**, C302 85.8% → **83.9%**.
- C302 두 몫의 합이 **189%** 다. 100% 를 크게 넘는 것은 두 변수가 **대체재**라는
  뜻이다 — 하나만 넣어도 대부분의 효과가 나오고, 둘 다 넣으면 하나만 넣은 것보다
  낮아진다. 구조적 원인은 `C302_CRI_ORD` 의 NaN 패턴이 이미
  `C302_MISSING_YN` 을 담고 있는 것이다 (LightGBM 이 NaN 을 별도 분기로 처리).
- **등급 서열 단독(105.1%)이 이력 유무 단독(83.9%)보다 높다.** 구세대의
  "C302 효과의 85.8%가 이력 유무" 라는 서술은 확정판에서 성립하지 않는다.
- ★ 위 값은 **시드 1회** 측정이다. 통합 σ ≈ 0.0014 이므로 비율의 불확실성이 작지
  않다 (분자·분모 모두 σ 를 가진다). 비율을 소수점 한 자리까지 단정하지 말 것.
  **"이력 유무가 절반 이상, 등급 서열이 그보다 크다"** 수준으로 기술한다.

## 15. [15번 승인] A축 원자료 복원 진행 중

`--run-all` 1시드 16종. 복원 중 확인된 재현 결과 (md 표와 대조):

| ID | md 표 | 복원값 | best_iter | 일치 |
|---|---|---|---|---|
| A0 | 0.8434 / KS 0.5307 / PSI 0.0167 / 4,813 | 0.8434 / 0.5307 / 0.0167 / 4,813 | 동일 | ✓ |
| A0c | 0.7826 / KS 0.4169 / PSI 0.0252 / 6,454 | 0.7826 / 0.4169 / 0.0252 / 6,454 | 동일 | ✓ |
| A1 | 0.8580 / KS 0.5520 / PSI 0.0307 / 4,215 | 0.8580 / 0.5520 / 0.0307 / 4,215 | 동일 | ✓ |

`best_iteration` 까지 완전히 같다. 나머지 시나리오도 같은 결과가 예상된다.
완료 후 A0~A7 각각의 gain 상위 15개를 JSON 에서 확보한다.

## 16. [17번 생략 승인] 다른 생성기의 동일 패턴 미점검

`step8`~`step27` 의 이전 세대 생성기는 이번 범위 밖이다. 기록:
`docs/07_한계와_향후과제.md` §5.

## 17. 문서 뼈대 작성 완료

| 파일 | 상태 |
|---|---|
| `README.md` | **작성 완료** — 한 줄 요약 / 무엇을 만들었나 / 핵심 결과 3층 / 데이터 표 / 폴더 구조 / 재현 방법 / 문서 안내 / 수치 인용 규칙 |
| `docs/01_문제정의와_데이터.md` | 뼈대 (목차 + 개요) |
| `docs/02_전처리_설계.md` | 뼈대 |
| `docs/03_모델링과_검증.md` | 뼈대 + **A5/A6 정의 전환 각주 작성 완료** |
| `docs/04_누수_발견과_제거.md` | 뼈대 (★ 핵심 문서. 원자료 경로 표 포함) |
| `docs/05_거시경제_결합.md` | 뼈대 + 매핑 사고 주의 문구 |
| `docs/06_비즈니스_임팩트.md` | 뼈대 + **구세대 수치 금지 목록 작성 완료** |
| `docs/07_한계와_향후과제.md` | **2·3·4·5절 작성 완료** (AC12 오염 / 측정일 의존성 / 재현성 사고 4건 / 미점검 범위) |
| `docs/appendix/` | step01~29 + `00_project_master_report.md` **이동 완료** (22개, 내용 수정 없음) |
| `docs/appendix/audit/README.md` | **작성 완료** — 검증 산출물 색인 |

### ★ `docs/appendix/audit/` 는 색인만 두었다 (판단)

감사·검증 문서는 대부분 **스크립트가 생성하는 산출물**이고 생성기가
`eda_pipeline/output/validation/` 에 쓴다 (`config.VALIDATION_DIR`, 7개 모듈이 참조).
파일을 옮기면 재생성 때 경로가 갈라져 **두 사본이 어긋난다.**
그래서 원본은 생성기가 쓰는 자리에 두고 `docs/appendix/audit/README.md` 를
**색인**으로 만들었다. 옮기려면 `config.VALIDATION_DIR` 변경이 선행돼야 하고
그건 별건이다.

### 이동에 따른 참조 갱신

`docs/step28` · `docs/step29` 를 가리키던 **주석 6곳**을 `docs/appendix/step*` 로
갱신했다 (`backend/grade_mapping.py`, `backend/routers/simulation.py`,
`eda_pipeline/step19` · `step22` · `step23`, `validation_common.py`).
모두 주석·독스트링이며 런타임 경로가 아니다. 이동 대상 문서의 **내용은 수정하지
않았다.**

---

# [2026-09-02] A축 원자료 복원 완료 · D7 측정 · 제안 58 확정

## 18. [15번 완료] A축 원자료 복원 — 15건 중 14건 완전 일치

`--run-all` 1시드 16종 실행. `ablation_A_results_R2.json` 에 **16개 시나리오
복원 완료** (A0 · A0c · A1~A8 · C0~C5).
검증 스크립트: `python -m eda_pipeline.verify_a_axis_restore` (재실행 가능).

| ID | 피처 md/복원 | best_iter md/복원 | Valid md/복원 | ΔValid | KS md/복원 | 판정 |
|---|---|---|---|---:|---|---|
| A0 | 152/152 | 4,813/4,813 | 0.8434/0.8434 | −0.00003 | 0.5307/0.5307 | 일치 |
| A7 | 162/162 | 3,493/3,493 | **0.9398/0.9398** | +0.00002 | 0.7387/0.7387 | 일치 |
| A3 | 154/154 | 3,823/3,823 | **0.9217/0.9217** | +0.00002 | 0.7013/0.7013 | 일치 |
| A0c | 150/150 | 6,454/6,454 | **0.7826/0.7826** | −0.00002 | 0.4169/0.4169 | 일치 |
| A4 | 153/153 | 4,766/4,766 | 0.8863/0.8863 | −0.00002 | 0.5973/0.5973 | 일치 |
| A2 | 156/156 | 4,916/4,916 | 0.8796/0.8796 | +0.00004 | 0.6078/0.6078 | 일치 |
| A1 | 153/153 | 4,215/4,215 | 0.8580/0.8580 | −0.00002 | 0.5520/0.5520 | 일치 |
| C1 | 151/151 | 4,103/4,103 | **0.8595/0.8595** | +0.00001 | 0.5552/0.5552 | 일치 |
| C5 | 114/114 | 5,572/5,572 | 0.8560/0.8560 | −0.00003 | 0.5498/0.5498 | 일치 |
| A5 | 151/151 | 4,926/4,926 | 0.8336/0.8336 | −0.00005 | 0.5090/0.5090 | 일치 |
| C4 | 150/150 | 6,276/6,276 | 0.8512/0.8512 | +0.00001 | 0.5505/0.5505 | 일치 |
| C0 | 150/150 | 6,130/6,130 | 0.8509/0.8509 | +0.00001 | 0.5483/0.5483 | 일치 |
| A6 | 151/151 | —/4,129 | 0.8465/0.8465 | −0.00004 | —/0.5313 | 일치 |
| A8 | 116/116 | —/4,300 | 0.8452/0.8452 | −0.00005 | —/0.5276 | 일치 |
| C2 | 151/151 | —/5,379 | 0.8431/**0.8432** | +0.00005 | —/0.5223 | 반올림 경계 |

- **`best_iteration` 까지 완전히 같다.** 확정판 md 표가 실측으로 재검증됐다.
  인용 대상인 **A7 0.9398 / A3 0.9217 / A0c 0.7826 / C1 0.8595** 전부 확인.
- **C2 1건만 차이**다 — `0.8431` vs `0.8432`, 폭 +0.00005. 4자리 반올림 경계
  (실제 0.84315)이고 σ(0.0014)의 3.6% 다. 실질 차이가 아니다.
  지시대로 **복원값 `0.8432` 를 정본**으로 하고 이 차이를 여기에 기록한다.
- C3 는 md 표에 없다 (정의상 A0 와 같은 구성). 복원값 0.8434 로 A0 와 일치한다.

### ★ gain 상위 15 확보 — 04 문서의 직접 근거

산출: `stage6_ablation/A_axis_gain_top15.json` (16개 시나리오).
**누수 변수를 넣으면 gain 상위가 어떻게 바뀌는지**가 그대로 보인다.

| 시나리오 | Valid AUC | gain 1위 | 2위 | 3위 |
|---|---|---|---|---|
| **A0** (시점 정합) | 0.8434 | `STD_INDS_MID2` 19.23% | `C302_MISSING_YN` 16.71% | `C302_CRI_ORD` 6.85% |
| **A2** (+CRIF 연단위) | 0.8796 | **`CRIF_EVENT_CNT` 18.63%** | `STD_INDS_MID2` 14.61% | `C302_MISSING_YN` 13.25% |
| **A3** (+CG01) | 0.9217 | **`CG01_MISSING_YN` 25.05%** | **`CG01_KIS_SCORE` 16.55%** | `C302_MISSING_YN` 13.57% |
| **A7** (누수 전부) | 0.9398 | **`CG01_KIS_SCORE` 53.45%** | `STD_INDS_MID2` 9.00% | `C302_CRI_ORD` 6.23% |

- 누수 변수를 넣는 순간 **그 변수가 바로 gain 1위로 올라온다.**
  A7 에서는 `CG01_KIS_SCORE` **단일 변수가 gain 의 절반 이상(53.45%)** 을 차지한다.
- 정합 구성(A0)에서 1·2위였던 `STD_INDS_MID2`(19.23%) 와
  `C302_MISSING_YN`(16.71%) 이 A7 에서 9.00% / 0.97% 로 밀려난다.
- A7 상위 15 에는 `COPR_OPNP_C`(3.40%), `CRIF_RLS_OCU_DT`(2.67%),
  `CRIF_WORST_RSNC`(0.74%), `CRIF_OVD_AM_SUM`(0.65%) 이 함께 올라온다 —
  되넣은 누수 변수들이 상위를 점령하는 구조다.

> **이 표가 04 문서의 핵심 그림이다.** "성능이 올랐다" 가 아니라
> "**무엇이 성능을 만들었는지**" 를 보여 준다.

## 19. [제안 58 확정] 확정판 기준 재산출 — 정본

복원 원자료에서 직접 계산했다 (`verify_a_axis_restore` 가 함께 산출한다).

| 분해 | 계산 | 몫 | 구세대 값 |
|---|---|---:|---|
| CG01 총효과 | `A3 − A0` = 0.92172 − 0.84337 | +0.07835 | — |
| **CG01 이력 유무 단독** | `A4 − A0` = +0.04291 | **54.8%** | 43.7% |
| C302 총효과 | `A0 − A0c` = 0.84337 − 0.78258 | +0.06079 | — |
| **C302 이력 유무 단독** | `A5 − A0c` = +0.05097 | **83.8%** | 85.8% |
| **C302 등급 서열 단독** | `A6 − A0c` = +0.06388 | **105.1%** | — |
| 합 | | **188.9%** | — |

**산출식은 구세대 값으로 역산 검증했다** — 구세대(`_R2_v1_preC302`, A0=0.7826)에
`(A4−A0)/(A3−A0)` 를 넣으면 **43.7%**, `(A6−A0)/(A5−A0)` 를 넣으면 **85.8%** 가
정확히 나온다. 당시 A5·A6 가 ADD 시나리오였기 때문이다. 확정판은 C302 가 A0 에
편입돼 있어 "둘 다 없음" 기준선이 **`A0c`** 로 바뀐다.

### 해석 — 구세대 서술은 확정판에서 성립하지 않는다

- 두 몫의 합이 **188.9%** 다. 100% 를 크게 넘는 것은 두 변수가 **대체재**라는 뜻이다.
  하나만 넣어도 효과가 대부분 나오고, 둘 다 넣으면 하나만 넣은 것보다 낮아진다.
  구조적 원인: `C302_CRI_ORD` 는 등급 이력이 없으면 NaN 이고 LightGBM 이 NaN 을
  별도 분기로 처리하므로 **그 결측 패턴이 이미 `C302_MISSING_YN` 을 담고 있다.**
- **등급 서열 단독(105.1%)이 이력 유무 단독(83.8%)보다 크다.**
  구세대의 "C302 효과의 85.8%가 이력 유무" 라는 서술은 **확정판에서 성립하지 않는다.**
  문서에는 이 서술을 쓰지 않는다.
- ★ **시드 1회 측정**이다. 통합 σ ≈ 0.0014 가 분자·분모에 모두 걸리므로 비율의
  불확실성이 작지 않다. 문서에는 소수점 한 자리로 단정하지 않고
  **"CG01 은 이력 유무가 절반 이상, C302 는 두 변수가 대체재이며 등급 서열이 더 크다"**
  수준으로 기술한다.

## 20. [D축 후속 2번] `fx_shock_x_export` 제거 측정 — 평균은 개선, **분산이 폭증**

D7 = D6 − `fx_shock_x_export` (거시 2종), D7m = D7 + 단조 제약. 시드 3회.

| ID | 피처 | Valid AUC(σ) | ΔAUC | 거시gain% | 상관(σ) | MAPE%(σ) | Brier | ECE | 판정 |
|---|---:|---|---:|---:|---|---|---:|---:|:---:|
| D0 | 156 | 0.8448(0.0006) | — | 0.00 | −0.1267(0.0346) | 62.78(0.78) | 0.011862 | 0.007745 | 기준선 |
| D6 | 159 | 0.8443(0.0006) | −0.0004 | 0.53 | −0.1010(0.0331) | 63.95(0.52) | 0.011897 | 0.007885 | 부분성공 |
| D6m | 159 | 0.8441(0.0004) | −0.0006 | 0.45 | −0.0697(0.0347) | 65.88(0.62) | 0.011830 | 0.008118 | 실패(MAPE) |
| **D7** | 158 | 0.8457(0.0006) | +0.0010 | 0.49 | −0.0695(**0.0952**) | 62.29(**5.09**) | 0.011913 | 0.007682 | 부분성공 |
| **D7m** | 158 | 0.8467(0.0020) | +0.0019 | 0.47 | **+0.0137**(**0.1246**) | **61.25**(**5.96**) | 0.011952 | **0.007550** | 부분성공 |

표면적으로 D7m 은 **처음으로 PD-부도율 상관 부호를 양수로 뒤집고**(+0.0137),
MAPE 가 D0 보다 낮으며(61.25 vs 62.78), ECE 는 전 시나리오 최저(0.007550)다.

### ★ 그러나 개선이 입증되지 않았다 — 시드 분산이 3~8배로 커졌다

| ID | 상관 범위 | σ | MAPE 범위 | σ | 보정 평균PD 범위 | 음수 시드 |
|---|---|---:|---|---:|---|---:|
| D0 | −0.1654 ~ −0.0990 | 0.0346 | 62.17 ~ 63.66 | 0.78 | 0.4418~0.4596% | 3/3 |
| D6 | −0.1333 ~ −0.0671 | 0.0331 | 63.46 ~ 64.49 | 0.52 | 0.4316~0.4445% | 3/3 |
| D6m | −0.1052 ~ −0.0359 | 0.0347 | 65.42 ~ 66.58 | 0.62 | 0.4063~0.4208% | 3/3 |
| **D7** | −0.1496 ~ **+0.0358** | **0.0952** | 58.09 ~ 67.95 | **5.09** | 0.3897~0.5104% | 2/3 |
| **D7m** | −0.0965 ~ **+0.1489** | **0.1246** | 55.85 ~ 67.65 | **5.96** | 0.3934~0.5382% | 2/3 |

- **양수 상관은 시드 하나(2024, +0.1489)가 만든 것이다.** 나머지 두 시드는 여전히
  음수(−0.0114, −0.0965)다. σ=0.1246 이므로 평균 +0.0137 은 0 과 구분되지 않는다.
- MAPE 개선(−1.53%p)도 2σ = 12.02%p 안이라 무변화다.
- 기전: D7m 의 `best_iteration` 이 3,976~5,106 으로 흔들리고 그에 따라
  **보정 후 평균 PD 수준이 0.39~0.54% 로 움직인다** (D6m 은 0.41~0.42% 로 안정).
  MAPE·상관이 그 수준을 그대로 따라간다.
- **gain 0.000 / 최하위였던 항을 뺀 것이 왜 분산을 키우는지는 이 실행으로 설명되지
  않는다.** 이것 자체가 확인이 필요한 관찰이다.

| # | 필요 조치 | 상태 |
|---|---|---|
| 18 | D6m · D7m 을 시드 7~10회로 재측정 — 분산 폭증이 실재인지 3시드의 우연인지 판별 | **승인 대기.** 단독 실행 시 20~30분 |
| 19 | 최종 구성을 D6m(3종, 안정) vs D7m(2종, 중심값 우수) 중 확정 | **승인 대기 — 18번 결과에 종속** |

> `step36_final_config.md` 는 이 결정 후에 작성한다. 근거 문장을 시드 하나에
> 걸어 두지 않는다.

---

# [2026-09-03] Group C·D 작업 중 파생된 미결 (기록만)

이번 라운드(C-1 벤 다이어그램 · C-2 문서 01~03 · D-1~D-3)에서 나온, **정정하지 않고
기록만 남기기로 승인된** 항목이다.

| # | 항목 | 상태 | 사유 |
|---|---|---|---|
| 20 | `docs/02` [^1] C302 등급적용시작일 4월 집중 연도별 건수(7,717 / 7,370 / 8,938) | **재집계 안 함 — 스킵 승인** | 코디네이터 제공값. 원자료 직접 재집계 필요 시 별도 지시 |
| 21 | `docs/01` [^2] 부도월 관찰등급(OBV) 부재율 48.6% / 376사 | **재측정 안 함 — 스킵 승인** | STAGE 3 버그 수정 기록의 인용. 현행 패널에서 재측정하지 않음 |
| 22 | `docs/03` §4-2 walk-forward · §4-4 알고리즘 패밀리 대조 | **정량 인용 금지** | step28 수치는 누수 제거 이전 구성. 방법론·정성 결론만 인용, D8 재산출은 향후 과제(07 §6) |
| 23 | `docs/01` [^3] 원천 raw 행수 JEMU 172,031 / OBV 972,155 (본문 171,845 / 970,459) | **정제 후 값 채택** | 조용히 고치지 않고 각주로 분리. docs/04 [^4] 와 동일 방식 |
| 24 | 거시 3층 구조 헤드라인 "63 → 11 → 14" 의 중간값 11 | **해결 (2026-09-03) — 정본 확정** | `92`·`11` 은 지시 과정의 착오값. 정본 5단계 = **63 → 97 → 15 → 10 (+완화 5) → 14**. docs/05 §7 선행에 5단계 표로 명시, 각주 [^3] 에 착오 경위 한 줄 추가. README·ppt 는 중간값 없이 "63개 원지표 → 부호 안정성 검증 → 상호작용항 14개" + 괄호 세부. 전 문서 grep 결과 "11개 통과" 표현은 [^3] 각주 언급 외 없음 |

`C1_venn_results.md` A 파트(내부 EWS 대조)는 `RZVL_POD` 11개월 한정으로 **삭제**했고
재현 스크립트에서 `WRITE_PART_A = False` 로 막았다. 상세는 07 문서 §4-12.
'내부 모형 AUROC 0.77 대비' 주장은 04 §5-2 · 06 §6-1 · 07 §4-12 에서 폐기 처리했다.
